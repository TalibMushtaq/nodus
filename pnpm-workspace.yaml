packages:
  - "apps/*"
  - "packages/*"
  - "scripts/*"
  - "services/*"
allowBuilds:
  esbuild: true
  # Native resolver pulled in by eslint-config-expo; its postinstall is safe.
  unrs-resolver: true
