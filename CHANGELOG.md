# Changelog

## [2026-09-11] - Tracker consistency: mark Phase 7b E2E complete

**What changed:** `Todo.md` Phase 7b now marks the S10 E2E item (`create code in UI → nodus node pair → node appears paired → WS challenge-response sync session succeeds`) as complete, matching `bootstrap-pairing-TODO.md` S10, which already recorded it done. Re-ran the full verification suite while auditing the tracker: `go -C services/relay test ./...`, `cargo test --manifest-path services/storage-node/Cargo.toml`, `pnpm test:ts`, `pnpm lint`, and `pnpm check-types` — all green.

**Why:** The two trackers disagreed on the final Phase 7b item; `bootstrap-pairing-TODO.md` explicitly requires the two lists to stay consistent. No code drift was found.

**Impact:** `Todo.md` (checkbox only). No code changed. `deploy/.env` confirmed untracked (only `deploy/.env.example` is committed), so no secret-hygiene action was needed.

**Follow-ups:** None.

## [2026-09-11] - S10 audit fixes (DB fast-fail, healthcheck, E2E robustness)

**What changed:** Follow-up to the S10 entry below. `services/relay/main.go`: `openDatabaseWithRetry` now fast-fails (`permanentDBError`) on positively permanent database errors — authentication/authorization (SQLSTATE class 28) and a missing database (`3D000`) — for both the pgx (`*pgconn.PgError`) and golang-migrate/lib/pq (`*pq.Error`) paths, instead of retrying a bad password for the full 60s; unclassified errors still retry so the fresh-deploy race keeps its budget (`lib/pq` promoted from indirect to direct). `deploy/docker-compose.yml`: the Relay healthcheck now requires the overall `"status":"ok"` rather than just `"postgres":"healthy"`, so a Redis outage no longer leaves the container marked healthy. `scripts/e2e-bootstrap-pairing.sh`: `psqlq` runs SQL through the Postgres container (`$POSTGRES_USER`/`$POSTGRES_DB`) instead of hardcoding `nodus`/`nodus_relay`, and the header states that minting goes through the same `/api/pairing/codes` proxy the browser dialog calls (the dialog itself is unit-tested). `bootstrap-pairing-TODO.md`: S10 wording/hardening notes updated.

**Why:** The S10 audit found the retry loop delayed a misconfigured deploy by a minute, the healthcheck ignored Redis, the E2E script broke under a custom `deploy/.env`, and the tracker/changelog overstated the browser UI coverage.

**Impact:** `services/relay/{main.go,go.mod}`, `deploy/docker-compose.yml`, `scripts/e2e-bootstrap-pairing.sh`, `bootstrap-pairing-TODO.md`. Verified: `gofmt`/`go vet` clean; rebuilt the relay image and re-ran the stack — `/health` healthy, and `scripts/e2e-bootstrap-pairing.sh` still **26 passed / 0 failed**; `pnpm test`/`lint`/`check-types` green.

**Follow-ups:** None.

## [2026-09-11] - S10: live E2E matrix + fresh-deploy/DB hardening

**What changed:** Ran the full bootstrap-pairing lifecycle against the live single-origin deploy unit and fixed two real issues it surfaced. (1) **Fresh-deploy Relay degraded-start race:** the Relay attempted Postgres once at boot; on a fresh volume Postgres' initdb briefly serves a *socket-only* temporary server, so the socket-based compose healthcheck could pass, the Relay would connect during the teardown gap, fail, and silently start in degraded mode with its auth/pairing routes unregistered (every `/api/auth/*`, `/api/pairing/*` → 404). Fixed with a bounded DB-connect retry in `services/relay/main.go` (`openDatabaseWithRetry`, 12×5s before falling back), a TCP Postgres healthcheck in `deploy/docker-compose.yml` (`pg_isready -h 127.0.0.1`), and a Relay healthcheck that requires `"postgres":"healthy"` (the bare `/health` 200 was returned even when degraded). (2) **Flaky concurrency test:** `TestRedeemConcurrentDoubleRedeem` could fail under the full integration suite because the process-global redeem rate limiter and a 250-address fake-IP helper let the draining rate-limit test leak a depleted bucket; fixed by resetting the limiter per integration test and reporting the offending status. Added `scripts/e2e-bootstrap-pairing.sh`, the reusable 26-check live matrix.

**Why:** S10 is the integration/E2E/hardening gate; the deploy race would break every fresh production install, and the flaky test would undermine CI trust.

**Impact:** `services/relay/main.go`, `deploy/docker-compose.yml`, `services/relay/internal/handler/pairing_code_test.go`, `scripts/e2e-bootstrap-pairing.sh` (new), `bootstrap-pairing-TODO.md`. Verified live from a wiped volume: Relay logs `postgresql: ready (migrations applied)`, and `scripts/e2e-bootstrap-pairing.sh` reports **26 passed / 0 failed** — happy path (mint → `node pair` → listed → WS auth → reconnect), consumed/expired/`node_owned_elsewhere` negatives, concurrent single-winner (one `200`, one `storage_nodes` row, code `CONSUMED` once), Relay-restart resilience, and re-pairing behavior. Full suite green: `pnpm test` (TS + Go + Rust), `pnpm lint`, `pnpm check-types`; Go integration tests green with `TEST_DATABASE_URL`/`TEST_REDIS_URL`.

**Follow-ups:** Migration complete (S1–S10). QR pairing remains a deferred v1 non-goal.

## [2026-09-11] - Docs S9 audit fixes (ADR citation, code comment, doc coverage)

**What changed:** Follow-up to the S9 entry below. `docs/decisions/0006-self-hosted-node-bootstrap-pairing.md`: the Context no longer claims ADR-0004 assumed QR-based LAN pairing (ADR-0004 is the mobile mDNS/WebRTC discovery decision and never discussed QR or node pairing); it now cites plan §7b "Non-goals" for the deferred QR option. `services/relay/internal/handler/pairing_code_gen.go`: corrected the stale comment that said the code space was `32^12 ≈ 2^60` — the generator emits 8 symbols, so it is `32^8 ≈ 2^40`, matching `docs/security/bootstrap-pairing.md`. `docs/security/bootstrap-pairing.md`: names the `pairing_codes` table and the operator-configured `PUBLIC_RELAY_URL` so the S9 proofread grep actually exercises all three terms.

**Why:** The audit found one incorrect ADR cross-reference, a code comment contradicting the new security doc, and an exit-criteria grep that matched only `node_not_found`.

**Impact:** `docs/decisions/0006-...md`, `docs/security/bootstrap-pairing.md`, `services/relay/internal/handler/pairing_code_gen.go` (comment only). `rg -n "pairing_codes|node_not_found|PUBLIC_RELAY_URL" docs/` now returns hits for all three; `gofmt` clean and `go -C services/relay build ./...` passes.

**Follow-ups:** None.

## [2026-09-11] - Docs: bootstrap-pairing ADR + security + protocol catalog (S9)

**What changed:** Added `docs/decisions/0006-self-hosted-node-bootstrap-pairing.md` (one-time `NODUS-XXXX-XXXX` bootstrap code, hashed at rest, atomic single-use redemption, Ed25519 identity retained; QR deferred) and indexed it in `docs/decisions/README.md`. Added `docs/security/bootstrap-pairing.md` documenting the two Relay endpoints (`POST /pairing/codes` authed mint, `POST /pairing/codes/redeem` open), hashed-only storage, 15-minute TTL, per-IP token-bucket rate limiting (burst 10 / refill 2-per-s, `TRUST_PROXY`-gated `X-Forwarded-For`), the full failure-reason table, and the threat model; `docs/security/README.md` now indexes the security docs and `local-endpoints.md` gained a scoped cross-reference section distinguishing Phase 11 device tokens from Phase 7b node bootstrap codes. Added a Node Authentication section (`node_auth_challenge`/`node_auth_response`/`node_auth_result` incl. the `reason` enum `node_not_found` | `node_inactive`) and an HTTP APIs section (`POST /pairing/codes`, `POST /pairing/codes/redeem` with statuses) to `docs/protocol/message-catalog.md`. Reconciled plan §7b drift: migration FK `ON DELETE` actions, the exact conditional-consume predicate, and both auth reasons.

**Why:** S9 requires the security posture and protocol surface to match the implementation so a first-time node bootstrap is auditable, and the tracker asked for the ADR + endpoint docs.

**Impact:** `docs/decisions/{README.md,0006-...md(new)}`, `docs/security/{README.md,bootstrap-pairing.md(new),local-endpoints.md}`, `docs/protocol/message-catalog.md`, `nodus_implementation_plan.md` §7b, `bootstrap-pairing-TODO.md`. Docs only — no code changed. Proofread pass over `docs/` for `pairing_codes` / `node_not_found` / `PUBLIC_RELAY_URL`; no code drift found (the Relay already emits `node_not_found` vs `node_inactive`; §3b routing was corrected in S8).

**Follow-ups:** S10 full lifecycle/negative/concurrency/restart E2E.

## [2026-09-11] - Deploy S8 audit fixes (relay routing breadth, origin defaults, tracker)

**What changed:** Follow-up to the S8 entry below. `deploy/Caddyfile`: the `@relay` matcher now covers every path a non-browser client answers directly — `/ws`, `/health`, `/rebuild`, `/auth/*`, `/devices/*`, `/nodes`, `/nodes/*`, `/pairing/*`, `/buffer/*` — instead of only the Storage Node subset, so the mobile client (which calls `/auth/login`, `/nodes`, `/devices/register`, `/pairing/sessions` directly) keeps working through the single origin. Bare `/devices` and `/auth` remain Next.js pages, with the Relay's device list reached via `/api/devices`. `deploy/docker-compose.yml`/`deploy/.env.example`: local `ALLOWED_ORIGINS` default now includes both `http://localhost` and `http://127.0.0.1`. `deploy/README.md`: updated routing diagram, clarified the page exceptions and exact-origin requirement, fixed the "Teardown" typo. `nodus_implementation_plan.md` §3b and `bootstrap-pairing-TODO.md`: route list/reconciliation updated. `Todo.md`: checked the S8 deployment items.

**Why:** The audit found the reverse-proxy sample omitted the Relay paths the mobile app calls directly, so a mobile client pointed at the public origin would 404 at Next.js; and the local quickstart was brittle when opened via `127.0.0.1`.

**Impact:** `deploy/{Caddyfile,docker-compose.yml,.env.example,README.md}`, `nodus_implementation_plan.md`, `bootstrap-pairing-TODO.md`, `Todo.md`. Validated: `caddy validate` → valid (and `caddy fmt` applied); `docker compose config` parses.

**Follow-ups:** The bare `GET /devices` Relay catalog remains reachable only via Next's `/api/devices`; a direct mobile caller of `/devices` (none today) would need `/api/devices` or an explicit relay-only origin.

## [2026-09-11] - Deploy: single-origin server unit + reverse proxy (S8)

**What changed:** Added the self-hosted deployment unit under `deploy/`: `Dockerfile.relay` (static Go build, migrations embedded), `Dockerfile.web` (monorepo install → `turbo run build --filter=web` → Next `standalone` runtime), a `docker-compose.yml` (postgres, redis, relay, web, caddy with healthchecks and volumes), `Caddyfile`, `.env.example`, and `README.md`; plus a root `.dockerignore`. `apps/web/next.config.js` now emits `output: "standalone"` with `outputFileTracingRoot` at the repo root so pnpm workspace packages are traced into the bundle.

**Why:** The stack must deploy as one public origin behind TLS with a real Storage Node pairing over the network. The S8 tracker text (`/api/*` → Relay) was stale: the web owns `/api/*` (session-cookie route handlers the browser calls) and proxies to the Relay internally, so forwarding `/api/*` to the Relay would break auth/pairing. Caddy now routes only the Relay-owned non-browser paths (`/ws`, `/buffer/*`, `/pairing/codes/redeem`, `/pairing/sessions/verify`, `/nodes/verify`, `/health`) to the Relay and everything else to Next — which also resolves the S5 seam (the node's `/pairing/codes/redeem` + `/buffer/fetch`).

**Impact:** `deploy/{Dockerfile.relay,Dockerfile.web,Caddyfile,docker-compose.yml,.env.example,README.md}` (new), root `.dockerignore` (new), `apps/web/next.config.js`, `README.md`, `nodus_implementation_plan.md` §3b. Verified live: `docker compose up --build` → `/health` and `/api/health` return ok, `/devices` redirects (Next), `POST /pairing/codes/redeem` reaches the Relay, a host Rust node paired via `--relay http://localhost` (redeem through Caddy) and WS-authenticated, and `GET /api/nodes` listed it. TLS is config-validated only (sandbox has no public domain); production sets `SITE_ADDRESS=<domain>` + `SESSION_COOKIE_SECURE=true`.

**Follow-ups:** S9 docs/ADR; S10 full lifecycle/negative/concurrency E2E. Redis is deployed but not strictly required by the Relay yet (keep for parity); `NEXT_PUBLIC_RELAY_URL` stays unset for the same-origin browser `/ws`.

## [2026-09-11] - Pairing S7 audit fixes (baseline race, copy feedback, tracker, newline)

**What changed:** Follow-up to the S7 entry below. `apps/web/app/(dashboard)/devices/devices-client.tsx`: the "+ Add Storage Node" action is now disabled while the node catalog is still loading, because the dialog snapshots `existingNodeIds` at mount and an empty/stale baseline would let `findNewNode` misread an existing node as a freshly paired one. `apps/web/components/add-storage-node-dialog.tsx`: `copyCommand` now returns early when `navigator.clipboard` is unavailable and only flips the check icon after a successful write, so the UI no longer claims a copy that did not happen. `Todo.md`: checked the S7 dialog/web-test items and recorded the issuance-vs-redeem divergence. `bootstrap-pairing-TODO.md`: restored the trailing newline lost in the S7 commit.

**Why:** The audit found a real (if narrow) race that could report a false "paired" success on open, plus a copy-feedback lie and two documentation/formatting nits.

**Impact:** `apps/web/{app/(dashboard)/devices/devices-client.tsx,app/(dashboard)/devices/__tests__/devices-client.test.tsx,components/add-storage-node-dialog.tsx}`, `Todo.md`, `bootstrap-pairing-TODO.md`. Test added: the Add button is disabled until the catalog resolves. `pnpm --filter web test` 71 passed (was 70); `check-types`, `lint`, `pnpm test:ts` green.

**Follow-ups:** Cross-session false positives remain inherent to the browser-only-issue design (the browser never learns the node id) and are documented; S10's live E2E will exercise the happy path.

## [2026-09-11] - Web: "+ Add Storage Node" pairing-code dialog (S7)

**What changed:** The Devices page can now bootstrap a Storage Node end-to-end. Added `apps/web/components/add-storage-node-dialog.tsx` (client): on open it mints a code via `createPairingCode()`, shows the relay URL (`PUBLIC_RELAY_URL`), the code, and the exact `nodus node pair --relay <url> --code <code>` command with a copy button, plus a live `MM:SS` expiry countdown. It polls `GET /nodes` every 3s and detects success by diffing against the node ids present when the dialog opened (the browser never learns the node's id ahead of time; `findNewNode` in `lib/pairing.ts` does the diff). State machine `creating → waiting → paired / expired / poll-error`; on paired it reports the node and `DevicesClient` re-syncs the catalog without a reload; expiry offers "Generate new code". The Storage Nodes section action is now "+ Add Storage Node" (opening the dialog) instead of routing to `/pair`; it is disabled with the existing warning when `PUBLIC_RELAY_URL` is unset. Built from `packages/ui` primitives (`Modal`/`ModalHeader`, `Input`, `Button`, `StatusBadge`, `Icon`) per the design-port conventions. Added `formatCountdown` to `lib/format.ts`.

**Why:** First-time pairing needs a guided path that hands the operator a code and the precise CLI command, then confirms the node actually registered — replacing the legacy `/pair` route for this bootstrap flow.

**Impact:** `apps/web/{components/add-storage-node-dialog.tsx(new),app/(dashboard)/devices/devices-client.tsx,lib/pairing.ts,lib/format.ts}`. Tests: new dialog suite (render/copy/countdown-expiry/poll success/poll failure/create failure/close), `findNewNode`, `formatCountdown`, and `DevicesClient` open/disabled cases — 70 web tests green; `pnpm --filter web test`, `check-types`, and `lint` clean.

**Follow-ups:** Redeem-time errors (`code_expired`, `node already paired`, `node_owned_elsewhere`) are CLI-only — the browser only issues the code, so the dialog surfaces issuance-side states; S9/S10 should reconcile that divergence. Manual browser E2E (open dialog → run CLI → watch flip to paired) remains for S10.

## [2026-09-11] - Pairing S6 audit fixes (proxy error surface, 2xx handling, env example, tracker)

**What changed:** Follow-up to the S6 entry below. `apps/web/lib/pairing.ts`: `createPairingCode()` now parses the Relay's `{"error": ...}` body on non-2xx and throws that machine-readable reason (falling back to the HTTP status), so S7 can render rate-limit/unauthorized states. `apps/web/app/api/pairing/codes/route.ts`: accepts any `2xx` as a successful mint instead of only `201`. `apps/web/.env.example`: commented out `NEXT_PUBLIC_RELAY_URL` and documented that it is a dev-only, bundle-inlined override — the same-origin `/ws` default is the production path. `Todo.md`: checked the S6 Next.js items, annotated the intentionally-skipped `codes/redeem` proxy and the node-revocation gap, and noted which web tests already landed in S6.

**Why:** The audit found the pairing proxy collapsed every failure to a bare status (hampering S7's error UI), treated a hypothetical `200` mint as failure, shipped an active client-visible `localhost` value in the env example, and left `Todo.md` out of sync with the session tracker again.

**Impact:** `apps/web/{lib/pairing.ts,app/api/pairing/codes/route.ts,.env.example}`, `Todo.md`. Tests updated/added: Relay error reason surfacing and a `200` mint case. `pnpm --filter web test` 58 passed (was 56); `check-types` and `lint` green.

**Follow-ups:** S7 renders the dialog on `createPairingCode()`/`findNode()`/`publicRelayUrl`; `PUBLIC_RELAY_URL` remains unvalidated (operator responsibility per §3b).

## [2026-09-11] - Web: pairing-code proxy + public relay URL plumbing (S6)

**What changed:** `apps/web` can now mint pairing codes and expose the operator's public relay URL without leaking internal config. Added `app/api/pairing/codes/route.ts` — a session-cookie-authenticated `POST` proxy for the Relay's `POST /pairing/codes` (relayFetch forwards `nodus_session`; 201 `{code, expires_at}` passes through, non-201 maps `{error}`; mirrors the existing `/api/pairing/sessions` route). Added `publicRelayUrl()` to the server-only `lib/relay.ts` (reads `PUBLIC_RELAY_URL`, trimmed; `null` when unset — never falls back to the internal `RELAY_URL`/localhost). The Devices page is now a server component that resolves `publicRelayUrl()` and passes it as a prop to a new client component `devices-client.tsx` (renders an "Pairing URL not configured" note when null; the S7 dialog will render the value). `lib/pairing.ts` gains `PairingCode`, `createPairingCode()` (posts to the new proxy), and the pure polling helper `findNode()`; `listNodes()` remains the poll primitive. `providers/ws-provider.tsx` no longer bakes `http://localhost:8080` into the client bundle: an explicit `NEXT_PUBLIC_RELAY_URL` still wins, otherwise the browser uses its own origin's `/ws`. `turbo.json` tracks `PUBLIC_RELAY_URL` and `NEXT_PUBLIC_RELAY_URL` in `globalEnv`, and `apps/web/.env.example` documents `RELAY_URL` (server-only), `PUBLIC_RELAY_URL` (user-facing, no default), and `NEXT_PUBLIC_RELAY_URL` (dev-only browser override), with a `.gitignore` opt-in for the example.

**Why:** First-time pairing from the web UI needs a way to mint a code under the account session and to show the exact public relay URL a user types into `nodus node pair` — while the internal `RELAY_URL` (dev default localhost:8080) must stay server-side so operators never see or bundle an internal address.

**Impact:** `apps/web/{lib/relay.ts,lib/pairing.ts,app/api/pairing/codes/route.ts(new),app/(dashboard)/devices/{page.tsx,devices-client.tsx(new)},providers/ws-provider.tsx,.env.example(new),.gitignore}`, `turbo.json`. Tests added/updated (10 files, 56 passing): `publicRelayUrl` env behavior, the codes proxy (201 pass-through + error mapping), `createPairingCode`/`findNode`/`listNodes` polling, the devices client (node catalog + unset warning), and the WS endpoint same-origin/override cases. `pnpm --filter web test`, `check-types`, and `lint` are green; a `next build` bundle check confirms neither the internal `RELAY_URL` sentinel nor `localhost:8080` appears in `.next/static`. Manual E2E: register through the web proxy then `POST /api/pairing/codes` returned a real `201 {code, expires_at}` from the dev Relay.

**Follow-ups:** S7 builds the "+ Add Storage Node" dialog on `DevicesClient`/`createPairingCode()`/`findNode()` and renders `publicRelayUrl`; S8 wires `PUBLIC_RELAY_URL` + proxy routing (`POST /pairing/*`) in the deploy unit and may add a dev `/ws` rewrite (the same-origin default otherwise requires `NEXT_PUBLIC_RELAY_URL` when the web app and Relay run on different ports). Node revocation has no Relay endpoint (`DELETE /nodes/{id}`); only device revocation exists.

## [2026-09-11] - Pairing S5 audit fixes (HTTPS guard, half-pair recovery, inactive-node reason, tracker)

**What changed:** Follow-up to the S5 entry below. `services/storage-node/src/pair.rs`: added `ensure_secure_transport`, which refuses to POST the pairing code to a non-loopback `http://` relay (loopback `localhost`/`127.0.0.0/8`/`::1` exempt for dev), returning the new `PairError::InsecureRelay`; a config-write failure after a successful redeem now returns `PairError::PersistFailed` instead of `Internal`, and `PairError::guidance()` returns case-specific advice for those two variants while keeping the not-paired guidance for genuine pairing failures. `sync/client.rs`/`sync/types.rs`/`main.rs`: the Relay now distinguishes an unknown node (`node_not_found`) from a registered-but-not-ACTIVE node (`node_inactive`); the node maps the latter to a new `NodeNotActive` marker and the sync loop prints "registered but not active … re-enable in the web UI, or re-pair if revoked" rather than misdirecting a paired node to `nodus node pair`. `services/relay/internal/handler/sync.go`: split the `err != nil || status != "ACTIVE"` branch into `node_not_found` (no row, message "storage node not found") and `node_inactive` (row exists, message "storage node is not active"). `packages/protocol`: `NodeAuthResultPayload.reason` enum now accepts `node_inactive` (regenerated `schemas/node_auth_result.schema.json`). `Todo.md`: checked off the S5 Rust/security items to match the session tracker.

**Why:** The audit found the bootstrap credential could be sent in cleartext to an arbitrary host, a failed local config write after redeem left the operator with an unexplained `code_consumed` on retry, and revoked nodes were told to re-pair as if unregistered. The checklist mirror had also drifted from `bootstrap-pairing-TODO.md` again.

**Impact:** `services/storage-node/src/{pair.rs,sync/client.rs,sync/types.rs,main.rs}`, `services/relay/internal/handler/{sync.go,sync_auth_reason_integration_test.go}`, `packages/protocol/{src/messages/control.ts,schemas/node_auth_result.schema.json,tests/messages.test.ts}`, `Todo.md`. New tests: HTTPS/loopback transport guard, tailored guidance, plaintext rejection before network, and `node_inactive` marker separation. `cargo test` green (125 bin + 116 lib unit, 2 integration), `cargo clippy -D warnings` clean; `go -C services/relay test ./...` green; `pnpm --filter @repo/protocol test` 54 passed.

**Follow-ups:** S8 reverse-proxy sample must route `POST /pairing/*` to the Relay (already noted in the tracker). Real-TTY `DialoguerPrompt` remains covered only through the `Prompter` trait; no automated PTY test.

## [2026-09-11] - Storage node: `nodus node pair` flow + unpaired UX + TLS (S5)

**What changed:** Implemented the first-time pairing flow. New `services/storage-node/src/pair.rs`: `nodus node pair` resolves relay + code (CLI `--relay`/`--code` non-interactive; `dialoguer` prompts defaulting the relay to the already-configured URL otherwise), reuses the persistent Ed25519 identity (never regenerates per attempt), POSTs `{code,node_id,public_key}` to `{relay_base}/pairing/codes/redeem` over HTTPS, and persists `relay_url` into `config.toml` **only after** a successful redeem. Relay HTTP status + `{"error":..}` bodies map to a typed `PairError` (`code_unknown`/`code_expired`/`code_revoked`/`code_consumed`/`node_owned_elsewhere`/`rate_limit_exceeded`/4xx/5xx/transport), each surfacing the machine-readable reason plus the "Storage Node is not paired. Run: `nodus node pair`" guidance. `main.rs` now factors the daemon into `resolve_config` + `boot_daemon`, so `node pair` continues straight into the running daemon (normal `/ws` challenge-response) after success, and its sync loop prints the pairing guidance (instead of a phantom relay outage) when the Relay reports `node_not_found`. `sync/client.rs` adds `relay_http_base` (public `https://host` or legacy `ws(s)://…/ws` → HTTP base; `relay_http_fetch_url` now builds on it, as does local discovery) and a `NodeNotPaired` marker error; `sync/types.rs` gains the optional `reason` field. `Cargo.toml` adds `rustls-tls-native-roots` to `reqwest` and `tokio-tungstenite`, which previously had **no TLS backend**, so HTTPS redeem and WSS sync actually work.

**Why:** The pairing code is the only bootstrap path for a self-hosted node; without this, nodes had no way to associate with an account and the daemon could not reach a production HTTPS/WSS Relay at all.

**Impact:** `services/storage-node/src/{pair.rs(new),main.rs,sync/client.rs,sync/types.rs,local/server.rs,config/mod.rs,identity.rs,lib.rs}`, `Cargo.toml`/`Cargo.lock`. Tests added: `relay_http_base` scheme mapping, `is_node_not_paired`, mock-relay redeem success + every failure reason, persist-on-success-only/exactly-once, identity reuse across attempts, interactive prompts (with configured default), and non-interactive missing relay/code. `cargo test` 120 unit + 2 integration green, `cargo clippy --all-targets -- -D warnings` clean. Manual E2E against the dev relay: `node pair` minted→persisted→booted and authenticated over WS, a subsequent `node start` reconnected without prompting, a consumed code failed with guidance, and an unpaired node printed the pairing guidance.

**Follow-ups:** S6/S7 web proxies + dialog; S8 must route `/pairing/*` (or expose a relay-only origin) to the Relay in the reverse-proxy sample, since the node appends `/pairing/codes/redeem` to its configured base while §3b's sample only routes `/api/*` and `/ws`.

## [2026-09-11] - Storage node: S4 audit fixes (relay validation, path/atomicity hardening, tracker reconciliation)

**What changed:** Follow-up to the S4 entry below, addressing audit findings. `services/storage-node/src/main.rs`: extracted the daemon relay guard into the pure `resolve_daemon_relay(Option<&str>)` helper, which now also rejects a configured value that does not normalize to a `ws(s)://` URL (e.g. `not-a-url`, a scheme-less host, or `ftp://`) instead of letting it spin in the reconnect loop; added tests for the missing-relay error, public-origin normalization, and malformed/bad-scheme rejection. `services/storage-node/src/sync/client.rs`: `relay_ws_url` now appends `/ws` under any base path (`https://host/proxy` → `wss://host/proxy/ws`) via the new `append_ws_path`, so the WS endpoint stays under the same prefix as the `relay_http_fetch_url` `/proxy/buffer/fetch` derivation; explicit `ws(s)://` URLs are still passed through verbatim and an already-`/ws` path is never double-suffixed. `services/storage-node/src/config/mod.rs`: `persist_relay_url` is now a read-modify-write over a `toml::Table` (preserving `data_dir` plus any key the `NodusConfigFile` struct does not yet model) written through a sibling temp file + rename so a mid-write crash cannot corrupt `config.toml`; added an unknown-key-preservation test. `Todo.md`: corrected the Phase 7b checklist, which had marked S5–S10 (pair flow, web client, E2E) as complete; each item now reflects its true state with a `— Sn` owner and a pointer to `bootstrap-pairing-TODO.md`.

**Why:** The audit found that the "empty-everything ⇒ error" exit criterion was only indirectly tested, an operator typo in `--relay` silently booted the daemon, subpath deployments produced mismatched WS/HTTP bases, and `persist_relay_url` could clobber future config keys or truncate on crash. `Todo.md` also contradicted the session tracker by claiming unbuilt work was done, which would mislead the next session.

**Impact:** `services/storage-node/src/{main.rs,sync/client.rs,config/mod.rs}`, `Todo.md`. `cargo test` now 109 unit + 2 integration tests green, `cargo clippy --all-targets -- -D warnings` clean. No behavior change for valid `https://`/`ws(s)://` relay values; a base-path public origin now yields `<base>/ws` instead of `<base>`.

**Follow-ups:** S5 implements `node pair` on top of these hardenings; monitor `relay_ws_url` subpath behavior against the S8 reverse-proxy sample.

## [2026-09-11] - Storage node: `node` CLI subgroup + relay URL precedence (S4)

**What changed:** Added a `node` CLI subgroup to `services/storage-node/src/main.rs` (`nodus node start`, `nodus node pair`) with the root flags `--data-dir`/`--force-adopt` now global, so bare/flag-only invocations and `node start` both boot the daemon as before. `node pair` is plumbed but returns a clear "lands in S5" error until the redeem flow exists. `relay_url` is now an optional key on `NodusConfigFile` and a resolved field on `Config`; precedence is CLI `--relay` > `config.toml relay_url` > `NODUS_RELAY_URL` > none via the pure `config::resolve_relay_url` (blank sources ignored), and `config::persist_relay_url` writes it back preserving `data_dir` (called only on successful pairing in S5). Removed the `NODUS_RELAY_URL` → `ws://127.0.0.1:8080/ws` fallback: the daemon now resolves via precedence and **fails loudly** when no relay is configured. Added `sync::client::relay_ws_url` to normalize an operator-facing public base (`https://host` → `wss://host/ws`, `http` → `ws`) while passing explicit legacy `ws(s)://…/ws` values through verbatim; the normalized WS URL feeds both the sync loop and local discovery.

**Why:** First-run pairing must never silently target localhost, and the pairing flow enters a public HTTPS origin that the WS-only sync client cannot dial unmodified. Locking one precedence function and one scheme-normalization helper in S4 keeps the S5 `node pair` implementation to the redeem/prompt/persist call-sites.

**Impact:** `services/storage-node/src/{main.rs,config/mod.rs,sync/client.rs}`; existing `config.toml` files without `relay_url` still parse (`#[serde(default)]`). New tests: relay precedence/blank handling, config round-trip + legacy parse + `persist_relay_url` read-back, `relay_ws_url` scheme mapping, and clap parse shapes (bare, root flags, `node start`, global flags after subcommand, `node pair --code`). `cargo test` 105 unit + integration tests green, `cargo clippy --all-targets -- -D warnings` clean.

**Follow-ups:** S5 implements `node pair` (dialoguer prompts, HTTPS `POST /pairing/codes/redeem`, identity reuse, `persist_relay_url` on success, failure-reason rendering); S6/S7 web proxies + dialog; no behavior change to node challenge-response.

## [2026-09-11] - Relay: harden pairing-code redemption (transactional consume, proxy-aware rate limit)

**What changed:** Reworked the open `POST /pairing/codes/redeem` handler in `services/relay`. The single-use consume and the `storage_nodes` upsert now run in one `pgx` transaction, so a rejected registration (`node_owned_elsewhere`) or any failure rolls back and leaves the code `PENDING` instead of burning it. Added an explicit `code_revoked` (410) branch, lenient `node_id` shape validation (non-empty, ≤128 printable ASCII) alongside the existing hex-32 `public_key` check, and default `capabilities` of `["storage","sync"]` for parity with `RegisterNode`. Fixed the per-IP rate limiter to key on the client IP (`net.SplitHostPort`) rather than `IP:port`, and added `TRUST_PROXY` (new env var, default off) to trust the first `X-Forwarded-For` value behind the operator's TLS reverse proxy; the route now receives `cfg`. Reconciled docs: dropped the unimplemented `node_claimed` failure from plan §7b, `Todo.md`, and `bootstrap-pairing-TODO.md`, documented the status set (400/404/409/410/429) and the transactional no-burn guarantee, and updated the S1 alphabet test description.

**Why:** An audit of the S2 redeem endpoint found (1) the rate limiter keyed on `IP:port`, so reconnecting bypassed the cap, and behind the single-origin TLS proxy every client shared one global bucket; and (2) consume-before-register with no transaction, which burned valid codes when registration was rejected.

**Impact:** `services/relay/internal/handler/{pairing_code.go,ratelimit.go,ratelimit_test.go,pairing_code_test.go}`, `internal/config/config.go`, `main.go`, plus `CHANGELOG.md`/`Todo.md`/`nodus_implementation_plan.md`/`bootstrap-pairing-TODO.md`. New env var `TRUST_PROXY`. `go -C services/relay test ./...` green (unit + integration against live Postgres), `go vet` clean.

**Follow-ups:** Inherited `is_primary` `NOT EXISTS` race for two distinct codes paired in parallel (also present in `RegisterNode`); rate-limiter bucket sweep if the map ever grows.

## [2026-09-11] - Marked first-time pairing (Phase 7b) as complete in Todo.md

**What changed:** In `Todo.md`, checked off all Phase 7b tasks (deployment model, relay migration 009 + code generation/redemption + rate limiter + `node_not_found` reason, Rust `node` CLI + URL precedence + `relay_url` persistence, Next.js proxy routes + `lib/pairing.ts` + Devices "+ Add Storage Node" dialog, and the Go/Rust/Web/E2E security suites) and the Phase 14 web first-time pairing item, on the assumption the first-time connection implementation is done. Phase 15 mobile pairing remains unchecked (Expo client not yet scaffolded). No code changed; this reflects a status assumption in the tracking file.

**Why:** The user is treating the first-time pairing flow as implemented and wants downstream tracking to reflect that.

**Impact:** `Todo.md` checkboxes only.

## [2026-09-11] - Added session-based tracker for the bootstrap pairing migration

**What changed:** Added `bootstrap-pairing-TODO.md` at the repo root — a session-by-session tracker (S1–S10) for the self-hosted Storage Node pairing-code bootstrap migration, mirroring `Todo.md` Phase 7b and plan §3b/§7b/§7c. Each session has a goal, checkbox tasks, exit criteria, and the exact verification commands (`pnpm test`, `go -C services/relay test ./...`, `cargo test --manifest-path services/storage-node/Cargo.toml`); a Status marker + Session Log support resuming across multiple sessions. Includes a Non-negotiables section (hash-only code storage, Ed25519 identity reuse, no localhost default, PUBLIC_RELAY_URL, atomic redemption, QR/central-relay non-goals).

**Why:** The migration spans Relay, Rust node, Next.js, deployment, docs, and E2E — a multi-session effort. Session-sized chunks with per-session exit criteria let each session start and end cleanly without re-reading the whole plan.

**Impact:** Documentation only — new root file; no code behavior changed.

## [2026-09-11] - Canonicalized self-hosted Storage Node pairing flow in the plan

**What changed:** Incorporated the final decision for first-time Storage Node bootstrap into `nodus_implementation_plan.md` and `Todo.md`. Added §3b (Self-Hosted Deployment, single public origin behind TLS, `PUBLIC_RELAY_URL` operator-configured), refined §7 "First-time pairing" into two distinct flows, added §7b (canonical pairing-code bootstrap: `NODUS-XXXX-XXXX` alphabet A-Z minus I/O + 2-9, ~15-min single-use, SHA-256-hashed `pairing_codes` table as migration 009, `POST /pairing/codes` auth + `POST /pairing/codes/redeem` open/atomic/IP rate-limited, reuses first-node `is_primary`) and §7c (`nodus node pair` CLI, URL precedence CLI `--relay` > `config.toml relay_url` > `NODUS_RELAY_URL` > no default). Added the bootstrap-only note to §8, `relay_url` persistence to §11/§11a, `pairing_codes` to §13, stage 7b + dependency note to §28, and marked the §29 "Pairing/QR format" item resolved (QR deferred). In `Todo.md`: inserted Phase 7b (deployment, migration 009, Go/Rust/Next/tests, non-goals), clarified Phase 11 as device↔node local pairing, reworded the Phase 14/15 QR-based pairing items to pairing-code entry (QR deferred), and resolved the "Pairing/QR format spec" open item.

**Why:** The implementation plan previously treated QR-based pairing as the default first-time flow and stored-node setup had no canonical account-association design; this decision set locks self-hosted deployment, pairing-code bootstrap, and the CLI/config precedence as the source of truth for implementation.

**Impact:** Documentation only (`nodus_implementation_plan.md`, `Todo.md`, `CHANGELOG.md`) — no code behavior changed. Existing opaque-session auth, Ed25519 node identity, and device↔node local pairing decisions preserved; QR-based pairing is now explicitly a v1 non-goal.

**Follow-ups:** Implement Phase 7b sub-tasks (relay migration 009 + generation/redemption handlers, Rust `nodus node pair` CLI, web "+ Add Storage Node" dialog, `PUBLIC_RELAY_URL` deployment unit); remove the `NODUS_RELAY_URL` localhost default in `services/storage-node/src/main.rs`; `NodeAuthResultPayload.reason="node_not_found"` for unpaired nodes.

## [2026-09-11] - Web dashboard: removed mock/stale data, real device & node data

**What changed:** In `apps/web`, the dashboard now renders only real backend data. Added `GET /api/devices` and `DELETE /api/devices/[id]` session-cookie proxies for the Relay's `GET /devices` and `DELETE /devices/{id}`; added `listDevices()` / `revokeDevice()` and the `RelayDevice` type to `lib/pairing.ts`; added `shortId` / `timeAgo` formatters in `lib/format.ts`. Rewrote `(dashboard)/devices/page.tsx`, `(dashboard)/security/page.tsx`, and `(dashboard)/overview/page.tsx` to fetch the real node/device catalogs with honest empty states, functional device revocation wired to the Relay, and a live WebSocket-aware relay status. Deleted `(dashboard)/files/page.tsx`, `(dashboard)/activity/page.tsx`, and `lib/mock-data.ts` (the file list, versions, activity feed, key envelopes, and recovery seed had no backend read path anywhere in the monorepo). Removed the fake "Files"/"Activity" nav items, the hard-coded "Alex Kim / alex@selfhost.dev" account, the static "Online · Local P2P" status chip (now real WS state + account_id from `useAuth`/`useWs`), the "+ New" button and dummy notification badge in `sidebar.tsx` / `app-shell.tsx` / `topbar.tsx`, and repointed device/security "Pair" actions to the real `/pair` flow.

**Why:** The dashboard was showing fabricated and stale values (mock node fleet, fake file counts, placeholder recovery seed, fake user identity) while the Relay already exposed the real device and node catalogs. Per ADR, only real-data-backed UI remains; sections whose data has no read path yet are removed rather than faked.

**Impact:** `apps/web` pages/components/libs/API routes; two dashboard routes and `lib/mock-data.ts` removed. `packages/ui` unchanged (unused mock-driven components remain exported but are no longer rendered). `pnpm --filter web` lint, check-types, and all 43 Vitest tests pass.

**Follow-ups:** Backend read paths for files/versions/activity (relay `GET /files`, `GET /files/{id}/versions`, activity feed) and key envelopes would let the Files/Activity/Security sections return; storage usage on nodes is not yet exposed by the Relay.

## [2026-09-11] - Storage node: quiet relay-down sync retries

**What changed:** In `services/storage-node/src/main.rs`, the sync loop now tracks a `relay_down_logged` flag and prints `sync: relay unreachable: <error>` only once per state transition instead of on every 5s retry; a successful session resets the flag so the next drop is logged again.

**Why:** A storage node run without the Relay (the default `ws://127.0.0.1:8080/ws`) previously spammed one `sync: session error` line every 5 seconds indefinitely. Web-side graceful degradation existed, but the Rust node had none.

**Impact:** `services/storage-node/src/main.rs` only; log-message behavior on `Ok` unchanged. No tests affected (log-only edit).

## [2026-09-10] - WebSocket and sync isolation hardening

**What changed:** Hardened Relay WebSocket identity and ingestion boundaries. WebRTC signaling now requires `from_peer` to match the authenticated node or device. Heartbeats derive presence exclusively from the connection identity, and node `last_seen_at` writes are throttled. Sync-event idempotency and snapshot staging conflict keys are account-scoped (migrations 007 and 008); file projection and snapshot public-key lookups now preserve account ownership. Storage-node-only message types are rejected from browser/device connections, and each Relay connection has a token-bucket message limit.

`RelayWsClient` now ignores callbacks from stale sockets, accepts JSON only in text frames (closing on binary/non-text frames), and treats sends while disconnected as no-ops. The web `WsProvider` retains subscriptions across client recreation and redirects authentication rejections to `/auth`.

**Verification:** Added focused Go and Vitest coverage for spoofed signaling, rate limiting, node-only routing, stale sockets, non-text frames, provider subscription continuity, and auth redirects. Full Go, TypeScript, Rust, build, lint, typecheck, and test suites pass.

## [2026-09-10] - Phase 14a: production-grade WebSocket client

**What changed:** Upgraded `RelayWsClient` (`packages/relay-client/src/ws-client.ts`) from a fire-and-forget wrapper into a stateful transport: a connection state machine (`disconnected → connecting → connected → reconnecting` with terminal `disconnected_max_retries`), automatic reconnection with exponential backoff via a new shared `backoffDelay`/`sleepBackoff`/`backoffDelayWithRandom` util in `packages/core/src/backoff.ts` (extracted from the duplicated copy in `packages/transfer-manager`, which now re-exports from `@repo/core`), a heartbeat loop (`peerId` now required at construction via new `RelayWsClientOptions`), presence announcement on connect using the protocol `heartbeat` message, a type-keyed `on`/`off` subscription layer on top of the legacy `onMessage` handler, idempotent `close()`, and auth-aware close handling via a dedicated `onAuthError` callback. The Go Relay (`services/relay/internal/hub/hub.go`, `handler/ws.go`) gained `CloseCodeUnauthorized = 4001`: browsers (identified by an Origin header) that fail the session-cookie handshake are closed immediately with that code instead of being left half-open, and `ReadPump` no longer logs it as an unexpected error. New `apps/web/providers/ws-provider.tsx` React context wires the client into the app (`status`/`send`/`on` via `useWs()`), constructed inside the effect so React StrictMode's mount→unmount→remount cycle pairs each client with its own cleanup; `<WsProvider>` wraps `<ThemeProvider>` inside `<AuthProvider>` in `apps/web/app/layout.tsx`. `apps/web` vitest config aliases relay-client to source; `@repo/core` was the only package still exporting raw TS source, so its `exports` map now points at `dist/` like every sibling package (required for Turbopack to bundle the backoff util). Tests: relay-client `ws-client.test.ts` grew from 21→43 tests (state machine, backoff/reconnect incl. max-retries terminal state, auth-rejection no-retry, heartbeat loop + dead-socket reconnect, presence, subscriptions, close idempotency); new `apps/web/providers/__tests__/ws-provider.test.tsx`; new Go `TestWebSocketRejectsUnauthenticatedBrowser`.

**Why:** Todo.md Phase 14 required reconnection, heartbeats, message routing, and presence in `packages/relay-client`; the previous client delegated reconnect policy to the caller and could not distinguish auth rejection from a transient network error.

**Impact:** `packages/core` (new `backoff.ts`, exports now dist-based — this is the notable packaging change), `packages/relay-client` (new deps `@repo/core`; expanded public API: `ConnectionState`, `RelayWsClientOptions`, `CLOSE_CODE_UNAUTHORIZED`, `on`/`off`, `onStateChange`, `onAuthError`), `packages/transfer-manager` (`./backoff.js` is now a re-export shim of `@repo/core`), `services/relay` (new WS close behavior — browsers with an invalid/absent session cookie are now rejected rather than silently kept open; storage-node challenge-response flow unchanged), `apps/web` (new provider + layout wiring). Breaking: `peerId` is required for automatic heartbeats/presence in the new options constructor; the legacy `(endpoint, handlers)` signature still works but does not auto-heartbeat.

**Follow-ups:** UI hook for `onAuthError` (redirect to login) is intentionally a no-op in the provider; presence roster/roster UI is separate front-end work. The relay-client reconnection backoff jitter reuses `baseDelay` (no separate jitter knob in the current options shape).

## [2026-09-09] - Phase 7a §6: client test suites

**What changed:** Added Vitest unit tests for the web client and relay-client packages. `apps/web`: added `vitest.config.ts`, `vitest.setup.ts`, and tests for `lib/device.ts` (identity generation, localStorage persistence, corruption recovery), `lib/auth-client.ts` (login/register/logout/fetchSession API calls), `lib/session.ts` (getSession/requireAuth with mocked relayFetch), and `providers/auth-provider.tsx` (AuthProvider component status transitions, login/register/logout handlers). `packages/relay-client`: added `tests/ws-client.test.ts` for `RelayWsClient` (relayWsEndpoint URL conversion, connect/send/close lifecycle, no `?token=` in URL). All tests use mocked fetch/WebSocket — no live Relay required.

**Why:** Todo.md Phase 7a §6 required client test suites covering the session-cookie auth flow, device auto-registration, and WebSocket handshake authentication.

**Impact:** `apps/web` (new Vitest deps + 4 test files), `packages/relay-client` (1 new test file), `Todo.md` (§6 items 1–3 checked). Mobile deferred to Phase 15.

**Follow-ups:** Mobile session model tests (Phase 15). Consider E2E tests with Playwright for end-to-end browser flows.

## [2026-09-09] - Phase 7a §5: cookie flags security tests

**What changed:** Added `TestAuthSessionCookieFlags` and `TestAuthSessionCookieSecureFlag` to `services/relay/internal/handler/auth_integration_test.go`. The first test asserts the always-on attributes (`HttpOnly`, `Path=/`, `SameSite=Lax`) on the `nodus_session` cookie during a normal login. The second test spins up a separate server with `SessionCookieSecure=true` (production mode) and verifies the `Secure` flag is set on the `Set-Cookie` header. Together they close the only gap in the Phase 7a §5 security test matrix; the remaining seven tests (expiry, revocation, fixation, hash-only storage, max-10 eviction, device-bound, throttle) were already in place.

**Why:** Todo.md Phase 7a §5 required cookie-flag assertions; the implementation was correct but untested.

**Impact:** `services/relay/internal/handler` test file only. No production code changed. `go vet` clean; tests require `TEST_DATABASE_URL` (integration gate). Todo.md §5 all items checked.

**Follow-ups:** Phase 7a §6 client test suites remain.

## [2026-09-09] - Web client: session-cookie pairing (Phase 7a §4)

**What changed:** Unified the Storage-Node pairing flow onto the session-cookie boundary, completing Phase 7a. `apps/web/app/pair` no longer builds `Authorization: Bearer` headers or reads a JWT from `sessionStorage` (the `nodus.jwt` artifact of the pre-§1 API is gone). The Relay has no CORS handling, so pairing calls are proxied server-side like §3: new `app/api/nodes/route.ts` (`GET /nodes`), `app/api/devices/register/route.ts`, and `app/api/pairing/sessions/route.ts` forward the HttpOnly `nodus_session` cookie via `lib/relay.ts relayFetch` and pass Relay errors through. `lib/pairing.ts` adds typed client helpers `listNodes()` / `registerDevice()` / `issuePairingToken(nodeId, device)` with `RelayNode`/`PairingSession` types mirroring the Go `NodeResponse`/`PairingSessionResponse` shapes. New `app/pair/layout.tsx` runs `requireAuth()` (matching the dashboard guard), so `/pair` redirects unauthenticated users to `/auth` and the page's old embedded sign-in form is deleted — section 1 now shows the session (`useAuth()`) with a sign-out action. The node catalog auto-loads once the session resolves (manual refresh button kept); "Issue pairing token" registers the browser device (idempotent ownership-safe upsert) and requests a device-bound token through the proxies before rendering the QR deep link. LAN discovery (`NodeClient.pair`/`authenticate`, `/nodus/discovery` probe) and trusted-node IndexedDB storage are unchanged — they address the Storage Node over the direct HTTP listener, outside the Relay's auth boundary. `packages/relay-client` audit: `RelayWsClient` opens a plain `WebSocket(url)` and authenticates via the host-scoped session cookie on the handshake (documented with a comment; no token in URL/headers), and `NodeClient`'s Ed25519 challenge-response is Bearer-free by design — nothing to change.

**Why:** spec (Todo.md Phase 7a §4) requires the pairing screen to sit on the same session-cookie boundary as the rest of the client — the browser must never hold a Relay token — and the old flow was broken against the §1/§2 token-free API.

**Impact:** `apps/web` + `packages/relay-client` (comment only). `/pair` was a static, unguarded page; it is now a server-guarded route proxy-backed route. Throws out the JWT-era `RELAY_BASE`/`sessionStorage["nodus.jwt"]` plumbing. Verified: `pnpm --filter web` lint (0 warnings), `check-types`, and `next build` all clean; live smoke test against a running Relay confirmed `/pair` redirects for an anon user and renders the paired-node list + issues a token for an authenticated session.

**Follow-ups:** Phase 7a §5 security tests, §6 client test suites. `GET /nodes/verify` + `POST /pairing/sessions/verify` stay out of scope (open, node-to-Relay endpoints the web client never calls).

## [2026-09-09] - Web client: session-cookie auth integration (Phase 7a §3)

**What changed:** Wired the Next.js client (`apps/web`) to the Relay's opaque-session API. New `app/api/auth/{login,register,logout,session}/route.ts` handlers proxy the Relay on the server (`lib/relay.ts`, `RELAY_URL` env, default `http://localhost:8080`) and forward its `HttpOnly; SameSite=Lax` `nodus_session` `Set-Cookie` verbatim, so one cookie is shared with the Relay and its `/ws` gateway. `lib/session.ts` adds server-side `getSession()` / `requireAuth()`; `lib/device.ts` persists the Ed25519 device identity (`nodus.device.identity`) via the existing `createDeviceIdentity` primitive; `lib/auth-client.ts` + `providers/auth-provider.tsx` expose a `useAuth()` hook (`login`/`register`/`logout`/`refresh`, status: loading/authenticated/unauthenticated). Route guards: `app/page.tsx` redirects `/overview` when authed else `/auth`; the `(dashboard)` layout runs `requireAuth()`. The `/auth` wizard dropped the mock `setTimeout`/TOTP/`localStorage["nodus-session"]` flow and now signs in or creates an account with the device id + public key in the body. `better-auth` was never installed, so its removal was already satisfied. `RELAY_URL` added to `turbo.json` `globalEnv`.

**Why:** spec (Todo.md Phase 7a §3) requires the browser to never hold the session token — it stays in the HttpOnly cookie managed by Next route handlers, with real (non-mock) auth entry points.

**Impact:** `apps/web` + `turbo.json` only. Requires the Relay running with `SESSION_COOKIE_SECURE=false` for plain-HTTP local dev (production uses `Secure`). `/pair` is left on its old Bearer/JWT form and is broken against the new API until Phase 7a §4. Verified: `pnpm --filter web` lint (0 warnings), `check-types`, `next build` all clean; live smoke test against a running Relay confirmed register→session→logout cookie lifecycle, guard redirects (`/` and `/overview`), and forwarded cookie flags.

**Follow-ups:** Phase 7a §4 pairing unification (`/pair` + `relay-client` Bearer removal), §5 security, §6 client test suites.

## [2026-09-09] - Relay: complete Auth API surface (Phase 7a §2)

**What changed:** Finished the session-cookie auth API from Phase 7a §1. `handler/auth.go`: `Login`/`Register` now auto-register the supplied device (required `device_id` + `device_public_key`) via an ownership-safe upsert and respond with the locked `{account_id, device_id, session_expires_at}` shape — no token in the body; `Register` mints the session cookie in the same call, and `GET /auth/session` reports the cookie-bound session (401 on missing/expired/revoked). `handler/device.go`: `upsertDeviceForAccount` refuses to re-assign a `device_id` owned by another account (`ON CONFLICT ... WHERE devices.account_id = excluded.account_id` → 409), closing a pubkey-overwrite gap in `RegisterDevice`; `RevokeDevice` now also kills every session bound to the revoked device via `RevokeAllForDevice`. `auth/session.go`: added `RotateSession` (new row + revoke old in one tx), the session-fixation primitive for future credential changes. `main.go` wires the store into `Register`/`RevokeDevice`. New `handler/auth_integration_test.go` (gated): register-mints-cookie, login auto-registers device, failed login does not mutate devices, cross-account device conflict, session 401s, logout clears cookie, device revocation invalidates sessions. Store tests added for `RotateSession` success + unknown-old failure.

**Why:** spec (plan §8/§13/§29, Todo.md Phase 7a §2) requires devices to auto-register alongside session creation, the `{account_id, device_id, session_expires_at}` response contract, and immediate session death on device revocation (fixation).

**Impact:** `services/relay` only; breaking Relay API change — login/register bodies now require `device_public_key`, response body replaces `expires_at`/`expires_in` with `session_expires_at`. Rust Storage Node auth untouched. `go test ./...` green with and without `TEST_DATABASE_URL`/`TEST_REDIS_URL`.

**Follow-ups:** Phase 7a §3–§6: Next.js cookie/route integration, pairing flow, security + client test suites. Password-change endpoint (triggers `RotateSession`) does not exist yet in the Relay and is wired when it lands.

## [2026-09-09] - Relay: opaque server-side session auth (§8/§13/§29, Phase 7a §1)

**What changed:** Replaced the Relay's JWT/refresh-token account auth with opaque server-side sessions. `internal/auth/token.go` lost `Claims`/`IssueAccessToken`/`ParseAccessToken`/`IssueRefreshToken`/`RotateRefreshToken`/`RevokeRefreshToken` and gained `GenerateSessionID` (32 bytes `crypto/rand` → base64url) + `HashSession` (SHA-256). New `internal/auth/session.go` defines a `SessionStore` interface and `PGSessionStore` with `CreateSession` (hash-only storage, 30-day `expires_at`, max-10-active eviction in one tx), `LookupSession` (rejects expired/revoked/inactive-device), `TouchSession` (≤1 `last_used_at` bump per 30 min), `RevokeSession`, `RevokeAllForAccount`/`RevokeAllForDevice`. `middleware.go` `RequireAuth` now reads the session cookie (`nodus_session`), hashes it, looks it up, and populates `AccountID`/`DeviceID` context (added `GetDeviceID`). `config.go` dropped `JWTSecret`/`JWTExpiry`/`RefreshExpiry`, added `SessionCookieName`/`SessionMaxAge`/`SessionTouchInterval`/`SessionCookieSecure`. Migration `006_sessions` creates `sessions` and drops `refresh_tokens` outright. `handler/auth.go` removed `RefreshToken` and token-bearing `AuthResponse`; `Login` now mints a device-bound session cookie, `Logout` revokes the row + clears the cookie, new `Session` handler backs `GET /auth/session`. `ws.go` removed `?token=` JWT parsing in favor of the session cookie (node Ed25519 challenge-response untouched). Routes: removed `POST /auth/refresh`, added `GET /auth/session`. Drafted unit tests (`GenerateSessionID`, `HashSession`, cookie middleware against a fake `SessionStore`) and `TEST_DATABASE_URL`-gated integration tests for the store (hash-only storage, expiry, revoked device, revoke-all, max-10 eviction, touch throttle). `golang-jwt/jwt/v5` removed from `go.mod`.

**Why:** The spec (plan §8/§13/§29, Todo.md Phase 7a) requires account auth as opaque, randomly generated server-side sessions — PostgreSQL stores only SHA-256 hashes and delivers a `HttpOnly; Secure; SameSite=Lax` cookie; no JWTs/access/refresh tokens anywhere.

**Impact:** `services/relay` only; breaking Relay API change (`POST /auth/refresh` removed, `GET /auth/session` added, token-free login/register bodies). Rust Storage Node auth (Ed25519 challenge-response, pairing tokens, `/buffer/fetch` single-use tokens) verified unchanged; `cargo test` green. `go test ./...` green with and without `TEST_DATABASE_URL`/`TEST_REDIS_URL`.

**Follow-ups:** Phase 7a §2–§6 land separately: device auto-registration on first login, `{account_id, device_id, session_expires_at}` response shape, session fixation rotation on credential/device change, Next.js + pairing integration, and the security/client test suites.

## [2026-09-09] - Docs: refresh README to reflect current implementation status

**What changed:** Replaced the README's "pre-implementation" framing with the real state of the repo: per-component status column (web client ported with mock auth, mobile scaffold, Rust/Go services implemented), corrected repository layout (added `packages/ui`, `packages/transfer-manager`, removed stubs), a "Current status" section summarizing Phases 0–13 + the design port, and a "Getting started" section covering prerequisites and how to run each component — web (`pnpm dev`), Relay (compose + `go run .`, with env-var table), Storage Node (`cargo run -- --data-dir`), plus a test command cheat-sheet.

**Why:** README.md still said "Nothing is implemented yet" after Phases 0–13, the design port, and the QA pass shipped.
**Impact:** Documentation only.

## [2026-09-09] - QA pass: accessibility + interaction fixes on the ported UI

**What changed:** Ran a visual/interaction/accessibility QA pass over the design port and fixed everything it surfaced (axe-core via CDN probe in both light and dark themes — zero remaining violations).
- **Fonts** (`apps/web/app/layout.tsx`): moved next/font `variable` classes from `<body>` onto `<html>` — they were undefined at `:root`, so `--font-sans`/`--font-mono` computed to an invalid (empty) value and the body silently rendered in the system sans face instead of Inter.
- **Interactions** (`apps/web/app/(dashboard)/files/page.tsx`, `overview/page.tsx`): wired the files table select-all checkbox (indeterminate, count "7 selected"); made overview stat cards and quick-access tiles navigate (`/files`, `/devices`, `/activity`).
- **A11y** (`packages/ui/src/primitives/{overlay,toggle}.tsx`, `domain/{file-row,pairing-modal}.tsx`, `apps/web/{components/{sidebar,topbar}.tsx, app/auth/page.tsx, app/(dashboard)/files/page.tsx}`): `role="dialog"`/`aria-modal`/labelled-by + Escape-to-close on modals, sr-only "Actions" header cell on the files table, aria-labels on icon buttons/search/pairing-code/row checkboxes, `aria-label="Primary"` on the sidebar nav, and `auth/page.tsx` `<div>` → `<main>`.
- **Contrast** (`packages/ui/tokens.css`, `apps/web/app/(dashboard)/overview/page.tsx`, `packages/ui/src/primitives/{stat-card,path-indicator}.tsx`): darkened light-mode `--color-accent`/`--status-pending`/`--status-synced` and muted-foreground, and brightened their dark twins; switched card/badge tint hairlines from hex-suffix alpha (`var(--x)18` — invalid, silently dropped) to `color-mix(...)`, and made the "Used"/Photos greens theme-aware via `var(--status-synced)`.

**Why:** The ported UI is the end-user surface; axe found real contrast failures (stat-card colored labels 2.9–4.2:1, dark muted text ~4.0:1) and the inert select-all was the one true interaction bug (the "missing" activity rows / inert pairing modal were hydration-timing artifacts in the test driver, not app bugs).

**Impact:** All six dashboard routes + `/auth` + `/` redirect are axe-clean (`color-contrast` and `empty-table-header` included) in both light and dark themes; server + client lint, `check-types`, and `pnpm build` still green.

**Follow-ups:** `/pair` remains untouched (deferred); auth stays a mock wizard until the real authenticated/session wiring (Phase 7a); the `color-contrast` `incomplete` notes in axe output are gradient/`background-image` surfaces axe cannot evaluate rather than violations.

## [2026-09-08] - Design port: prototype UI shipped in Tailwind v4 with centralized packages/ui

**What changed:** Ported the `nodus-design/` Figma prototype into the Next.js app. A complete shared component library was built in `packages/ui` (design tokens + 17 primitives + 13 domain panels, all Tailwind v4 CSS-first) and consumed by six new dashboard routes plus a single-step mock auth flow:
- **Tokens** (`packages/ui/tokens.css`): `@theme` with the warm neutral palette (Inter + JetBrains Mono), `.dark` as a designed second mood, status-palette custom properties, and the `accent-gradient` / `surface-warm` / `topbar-warm` utilities with dark twins.
- **Primitives**: `icons.tsx` (28 centralized SVG glyphs), `button` (primary/secondary/ghost/destructive/link), `input`, `select`, `checkbox`, `toggle`, `field`, `badge` (StatusBadge), `path-indicator` (local/relay/buffered/offline), `progress`, `segmented`, `overlay` (Modal/Drawer + header), `brand` (Logo/Avatar), `section`, `setting-row`, `breadcrumb`, `filter-chips` (FilterChips + TransferStatus), `pagination`, `stat-card`, `empty-state`.
- **Domain panels**: `file-row`, `file-detail-panel` (+ version history), `conflict-modal`, `device-row`, `node-row`, `pairing-modal`, `network-topology`, `recovery-key-card`, `key-envelope-table`, `device-revocation-list`, `activity-table`, `storage-usage-bar`, shared `types.ts`.
- **App**: `(dashboard)` routes (overview/files/devices/activity/security/settings) wired into a URL-driven `AppShell` (sidebar + topbar, collapse state, `.dark` fixes for gradients), `lib/mock-data.ts`, `/overview` root redirect, and a mock email→password→TOTP auth wizard at `/auth` (deferred real auth per Phase 7a).
- **Sidebar/topbar/app-shell live in `apps/web/components`**, not `packages/ui` — they depend on `next/link` + pathname, so they are intentionally app-layer, while every reusable/design component stays in the shared package.
- Fonts switched Geist → Inter/JetBrains Mono (`next/font/google`); deleted boilerplate fonts/SVGs/page.module.css.

**Why:** The design port was the next milestone — it consumes Tailwind v4 tooling set up earlier (tokens.css/theme-provider were already committed) and centralizes the prototype's visual language in `packages/ui` so future screens stay consistent. Auth wiring stays mock because the real opaque-server-side-session migration is deliberately deferred to Phase 7a.

**Impact:** `pnpm --filter ui lint|check-types`, `pnpm --filter web lint|check-types`, and `pnpm --filter web build` are all green; all 12 routes prerender as static pages. Verified custom utilities/tokens land in the compiled CSS. One real structure change: the `@repo/ui` package exports now map `"./*" → "./src/*.tsx"` with an explicit `"./domain/types" → "./src/domain/types.ts"` entry (TS `Bundler` resolution cannot append extensions to wildcard-export targets, so `.ts`-only files need explicit subpath exports). The working `apps/web/app/pair` page is untouched (restyle deferred); conflict resolution, details panel, pairing, and revoke are wired to working mock state.

**Follow-ups:** Visual QA pass across 6 pages in light/dark + collapsed sidebar, `/pair` restyle, and `docs/design-port-plan.md` detailing where each prototype screen maps in the new structure.

## [2026-09-08] - Plan: opaque server-side session auth migration (replace JWT/refresh tokens)

**What changed:** Documentation-only refactor of the auth roadmap. Updated `nodus_implementation_plan.md`: added the four-layer identity matrix (account session vs device key vs node key vs file encryption keys) to §8, clarified in §9 that the app password is Argon2id-hashed and never a bearer/network credential, replaced the `refresh_tokens` model with a `sessions` table + locked session-lifecycle semantics in §13 (30-day absolute lifetime, `last_used_at` bumped at most once per 30 min, max 10 sessions/account, immediate revocation, every session bound to an auto-registered device), inserted step 7a into the §28 implementation order, and resolved the §29 auth design decisions (no JWT/access/refresh tokens, no Better Auth, session-cookie WS auth, `?token=` removed, `GET /auth/session` added, `/auth/refresh` removed). Rewrote the root `Todo.md` with a new **Phase 7a** auth-migration checklist (Relay backend, auth API, Next.js integration, pairing, security + client tests) plus explicit non-goals.

**Why:** The user's spec requires opaque server-side sessions with PostgreSQL storing only SHA-256 hashes and cookie-based auth, with no JWT/access/refresh tokens anywhere — so the out-of-date JWT-planning had to be replanned and tracked before any code migration. `accounts.password_hash` was already Argon2id (requirement already met).

**Impact:** No production code changed. Affects planning docs and the migration checklist only; `services/relay` auth code (`token.go`, `middleware.go`, `handler/auth.go`, `config.go`, migration `001`), `apps/web/app/pair`, and `packages/relay-client` are the entities slated for the migration. Breaking API change planned: `/auth/refresh` removed, `GET /auth/session` added, WS auth via cookie instead of `?token=`.

**Follow-ups:** Code migration itself is intentionally deferred — do not implement until explicitly asked.

## [2026-09-07] - Transfer Manager: repair data-return path (P2) + object_id threading (P3)

**What changed:** Finished the §21a repair receive path behind the Option B backhaul (mDNS + authenticated HTTP). P3: `object_id` (the BLAKE3 content hash) is now threaded end-to-end — `ShardTransferRequest`/`TransferResult` gained `object_id: String`, `fetch_shard(peer, object_id)` takes it directly, and `DegradedShard` was slimmed to `object_id` (its `file_id`/`version_number`/`shard_index` were unread once fetch became object-addressed; the `find_degraded_shards` query selects `DISTINCT s.object_id`). P2: `TransferResult` gained `data: Vec<u8>`, and the dead `TODO(receive-path)` in `reconcile.rs` `run_repairs` is replaced by a real restore: on success it BLAKE3-verifies the fetched bytes against `object_id`, atomically writes them (`<data_dir>/objects/<ab>/<object_id>` via same-directory temp-file + rename), then flips `storage_objects` `DEGRADED → STORED`.
- **Receive endpoint** (`src/local/server.rs`): new `GET /nodus/shard/{object_id}` with stateless node auth — the requester signs `"{node_id}:{object_id}:{timestamp_ms}"` and sends it in `X-Nodus-Node-Id`/`X-Nodus-Timestamp`/`X-Nodus-Signature` headers; the receiver looks the caller up in `trusted_nodes`, enforces a ±60 s freshness window, and maps failures to `401 UNAUTHORIZED` (unknown/stale/bad signature) or `404 NOT_FOUND`. The previously-`#[allow(dead_code)]` `store` in `LocalState` is now used.
- **Peer discovery + Path A** (`src/local/mdns.rs`, `src/transfer/node_attempter.rs`): added an mDNS *browser* (`mdns = "3"`, pure-Rust; libmdns advertises only) with `discover_node(node_id)` matching the responder's `node_id=…` TXT record; `NodePathAttempter` carries a `reqwest::Client` and its `LocalSignaling` arm now does discover → sign → `GET /nodus/shard/{object_id}` → populate `data`, instead of the hard-failure stub. Paths B/C/D remain honest failures for node→node repair.
- **TS parity** (`packages/transfer-manager/src/types.ts`): `TransferResult` gained optional `data?: Uint8Array` and `objectId?: string`.
- **Spec** (`docs/architecture/transfer-manager-spec.md`): rewrote the Re-fetch-from-peer section for the v1 behavior (single best-peer fetch, verify + atomic restore + STORED) and documented the node→node Path A HTTP transport + header contract.

**Why:** P2 and P3 were the two blockers for a working repair-receive path: `TransferResult` had no way to carry received bytes, and `fetch_shard` took file/version/shard coordinates while the restore loop needed the content hash it fetches and writes under.

**Impact:** A DEGRADED shard can now be repaired end-to-end when a peer holds a copy and both nodes advertise on the same LAN: mDNS discovery → signed HTTP fetch → hash-verified atomic restore → `STORED`. Because the receiver only serves `trusted_nodes` callers and the requester only writes bytes that hash to the requested `object_id`, a malicious/compromised peer cannot plant arbitrary content. Non-LAN wiring (Path B/C/D receive) and peer-IP caching remain future work. Tests: 90 unit + 2 integration (Rust, +2 for the authenticated shard-fetch endpoint incl. trust/signature/staleness/404 negatives), 13 vitest (TS) + lint + check-types all green; `cargo fmt --check` + `clippy -D warnings` clean.

**Follow-ups:** Path B (Relay signaling), Path C (buffer), and Path D still fail honestly for node→node repair — wiring those receive directions is the remaining Phase 13 work. Also note: `trusted_nodes` currently has **no production write path** (every INSERT is in a `cfg(test)` module, and the path cache only UPDATEs), so a node starts with an empty peer set and `trusted_peers_for_repair` finds no one until a seed producer lands — e.g. recording peers encountered during client-initiated transfers, or registering known nodes at pairing/setup.

## [2026-09-07] - Transfer Manager: submit_repairs fan-out fix, graceful semaphore, comment cleanup

**What changed:** `submit_repairs` in `reconcile.rs` now submits one `fetch_shard` per DEGRADED shard to the best-candidate peer (first entry in the ordered `peers` vec) instead of fanning out to all `N×M` peers simultaneously — the executor has no peer-fallback loop, so multiple concurrent fetches of the same shard merely burn pool slots. Updated the §21 test to assert the single-fetch behavior. `pool.rs` `ConcurrencyPool::submit` no longer panics with `expect("semaphore closed")` on shutdown; instead it gracefully drops the transfer (the caller's oneshot receiver resolves with `RecvError`). Fixed a misleading comment in `manager.rs` `fetch_shard`: the comment now explains that `data`/`hash` are response fields and remain empty in the outgoing request.

**Why:** The `N×M` fan-out wasted bandwidth and could starve the 4-slot pool on duplicative requests for the same shard. The semaphore panic risked a crash during graceful shutdown. The original "Populated by the attempter" comment was inaccurate for fetches.

**Impact:** No behavior change to working paths (node→node receive is still unimplemented); repairs now request only 1 fetch per shard per scan, making the pool usage linear in degraded-shard count and eliminating unnecessary peer traffic. `pool.rs` shutdown path is panic-free. Tests pass (88 unit + 2 integration).

**Follow-ups:** P2 (data return path in `TransferResult`) and P3 (`object_id` threading through `fetch_shard`) remain the two blockers for a working repair-receive path; both depend on the yet-unimplemented node→node receive architecture.

## [2026-09-07] - Transfer Manager verification fixes

**What changed:** Post-implementation audit of Phase 13. Removed the dead-end `pending_repairs` buffer from `NodePathAttempter` — Path C/D attempts were pushed into a separate vector that nothing ever drained, while the manager's `MemoryLocalQueue` (the thing `drain_queue()` retries) stayed empty. The attempter now returns honest hard failures for all four paths (PHASE A/B unimplemented receive/signaling, C/D rewired retry semantics: repairs are re-driven by the scheduled reconciliation scan, not the memory queue). The reconcile repair loop no longer discards its results: `run_repairs` logs each repair outcome (success/failure/aborted) and carries an explicit TODO for the DEGRADED→STORED flip once a receive path lands. Added Rust tests for `MemoryLocalQueue` (FIFO, remove, empty) and a `TransferManager` test proving `enqueue` → `drain_queue` re-submits into the pool (88 unit + 2 integration tests pass). Documented in `transfer-manager-spec.md` that the Rust jitter term is inclusive `[0, jitter]` vs TS exclusive `[0, jitter)` (inclusive avoids a `gen_range(0..0)` panic at jitter=0), and made the crate `clippy -D warnings`-clean: dropped the fire-and-forget receiver explicitly (`std::mem::drop`), collapsed the nested `if`s in `webrtc_client.rs`'s ack handler to let-chains, and annotated `TransferPath::from_str`. `cargo fmt` reformatted previously non-rustfmt-clean test inserts.
**Why:** The audit found Path C/D repair requests were being silently parked in an unreachable buffer (data that looked queued was actually lost), and repair results were discarded with no observability. The remaining items were strictness/parity nits that would have blocked the pre-push clippy gate and diverged the two implementations' jitter semantics.
**Impact:** No behavior change to working paths (Paths A–D still all report failure on the node side today) — this closes the dead-storage trap so a future receive-path implementer won't build on a misleading queue, gives the repair loop actionable logs, and makes `pre-push` (rustfmt + `clippy -D warnings`) pass before the next commit.
**Follow-ups:** The DEGRADED→STORED flip on successful repair (TODO in `reconcile.rs`) plus node→node WebRTC receive remain the real remaining Phase 13 work; re-run the full `cargo test` + `pnpm --filter @repo/transfer-manager test` before the next push.

## [2026-09-07] - Phase 13: Transfer Manager

**What changed:** Delivered the Transfer Manager — a bidirectional, per-shard transfer orchestrator with a 4-path fallback chain (Local signaling → Relay signaling → Buffer-and-Relay → Local queue), a persisted path cache, a bounded concurrency pool (default 4), and exponential backoff with jitter — on both the TS client and the Rust Storage Node, wired into the §21a re-fetch-from-peer repair action.
- **Spec** (`docs/architecture/transfer-manager-spec.md`): shared TS/Rust contract — `TransferPath` (4 values, byte-identical string names), `TransferState`, failure taxonomy (transient/permanent, cached-path failure evicts without retry), timeout table (local discovery 2000ms, WebRTC negotiation 4000ms, relay signaling 3000ms), backoff `base*2^attempt + [0, jitter)` (base 500ms / jitter 300ms / max 2 retries per stage), concurrency, path-cache persistence, and the generic Path D `LocalQueue` interface (memory implementation for v1). Decisions locked: fresh mDNS each time (never cache peer IPs), §21 peer selection is brute-force try-all trusted nodes ordered by path-cache hit then recency (no `peer_shard_locations` index).
- **Migration** (`services/storage-node/migrations/20260907000001_transfer_manager_cache.sql`): adds `last_successful_path`/`last_success_at` to `trusted_nodes` for the cached `UPDATE`-style cache writes.
- **TS package** (`packages/transfer-manager`): `TransferManager` (uploadShard/downloadShard/enqueue/notifyConnectivityRestored), `ConcurrencyPool` (FIFO + max concurrency), `executeTransfer` (cache-first chain, immediate evict on cached-path failure), `InMemoryPathCache`, `MemoryLocalQueue`, `makeConfig`, `backoffDelay`/`sleepBackoff`. 13 vitest tests + lint/check-types green.
- **Rust node** (`services/storage-node/src/transfer/`): mirror modules (`types`, `config` with `NODUS_TM_*` env overrides, `backoff`, `cache` on `trusted_nodes`, `queue`, `executor`, `pool` on tokio `Semaphore`, `webrtc_client` `initiate_transfer` SDP offerer, `manager` `fetch_shard`/`push_shard`/`enqueue`/`drain_queue`, `node_attempter`). `NodePathAttempter` returns honest failures for Path A/B (node→node WebRTC receive and node→node relay signaling not yet supported — the Phase 11 `WebRtcManager` accepts paired devices only) and queues for Path C/D. Registered in `lib.rs` + `main.rs`; 4 executor/pool/cache unit tests added alongside the existing backoff/cache tests.
- **§21 repair wiring** (`services/storage-node/src/store/reconcile.rs`): `DegradedShard`, `find_degraded_shards` (shards backed by `DEGRADED` objects), `trusted_peers_for_repair` (path-cache first, then `last_success_at` desc), and `submit_repairs` (one `fetch_shard` per shard×peer). `spawn_reconcile_task(store, interval, Option<TransferManager>)` runs repairs on boot + each scan; main.rs constructs the manager (Arc'd node identity, `SqlitePathCache`, `NodePathAttempter`) and passes it in. `main.rs` dead-code warnings silenced at the transfer-module level.
- **Benchmark harness** (`scripts/benchmark-webrtc`): CLI half (`pnpm benchmark:timings`) verifies the deterministic timing contract (backoff growth + timeout table); browser half (`bench.html`) measures real Path A/B WebRTC negotiation over native `RTCPeerConnection` and emits structured JSON for `docs/architecture/webrtc-benchmark-results.md`. `pnpm-workspace.yaml` now includes `scripts/*` + approves the `esbuild` build script.
- **Why:** The node had no unified transfer story — each path (local WebRTC, relay signaling, buffer-and-relay) was a one-off, with no retry/backoff/cache/concurrency semantics and no path-preference memory. The Transfer Manager centralizes that machine and lets the §21a repair action reuse identical fallback behavior instead of bespoke logic.
- **Impact:** Repairs from reconciliation now flow through the same fallback chain as client transfers, with persisted per-peer path preference; both languages share one documented contract, so drift is caught by the spec and the TS/Rust mirror tests. Known gap (same on both sides): node→node direct receive and relay signaling still need a peer-facing WebRTC receive path (a later phase) — until then repairs degrade honestly through the local queue.
- **Follow-ups:** Node→node WebRTC receive (PeerConnection answer side on the node) will unlock Paths A/B for repairs; wire `notifyConnectivityRestored` from the sync loop so the Path D queue drains on real connectivity events (TS: `TransferManager.notifyConnectivityRestored` is wired to the queue already; Rust has the hook, no caller yet); run the browser benchmark matrix and append results to `docs/architecture/webrtc-benchmark-results.md`; Phase 14 plugs persistent cache/queue backends (`IndexedDB`/SQLite) behind the existing interfaces.

## [2026-09-06] - Fix VerifyPairingSession token consumption + spec TTL alignment

**What changed:** `services/relay/internal/handler/pairing.go` `VerifyPairingSession` now atomically redeems the token: the plain `SELECT` is replaced with `UPDATE pairing_sessions SET consumed_at = NOW() WHERE token = $1 AND status = 'ACTIVE' AND consumed_at IS NULL AND expires_at > NOW() RETURNING account_id, node_id, device_public_key, expires_at`. A matched row returns `valid: true` and is consumed in the same statement (concurrent verifies race on the row lock, so only one caller succeeds); an unknown, already-consumed, or expired token yields no rows and returns `valid: false`. Added `services/relay/internal/handler/pairing_test.go` with integration tests for both `CreatePairingSession` (success, node-not-found, device-not-found) and `VerifyPairingSession` (valid consumes token, replay rejected, expired rejected, unknown rejected). Updated the pairing-token TTL in the specs from 5 to 15 minutes to match the implementation: `docs/security/local-endpoints.md` ("TTL: 15 minutes") and `docs/protocol/local-discovery.md` ("15 min TTL"). Fixed a comment typo ("endpooints" → "endpoints").
**Why:** The verify fallback path never consumed tokens — a token redeemed over `POST /pairing/sessions/verify` stayed `ACTIVE` in the Relay DB and could be replayed until `expires_at`, breaking the single-use invariant in `docs/security/local-endpoints.md` ("the first consumer atomically sets consumed_at"). The spec's 5-minute TTL contradicted the shipped 15-minute `pairingTokenTTL` (already documented in this changelog), so the docs were aligned to the working implementation rather than changing behavior.
**Impact:** The Relay's `/pairing/sessions/verify` is now the source of truth for single-use across the hybrid fast/fallback flow (design decision E); a token verified via the fallback cannot be re-verified. Existing behavior for `CreatePairingSession` and `VerifyNodeURL` is unchanged. Six new integration tests run under `TEST_DATABASE_URL` (skipped when unset), consistent with the existing harness pattern.
**Follow-ups:** Confirm the node's fast-path `account_id = "local_push"` pairing is never verified against the Relay table (it is local-only by design); if offline-pair tokens ever need Relay-side revocation, expose a revoke path — token single-use already covers normal replay.

## [2026-09-06] - Phase 11: Local discovery, pairing & node authentication

**What changed:** Implemented the LAN trust boundary end-to-end: a mDNS + plain-HTTP listener on the Storage Node, token-bound device pairing via the Relay (with an offline fast-path), a shared TypeScript client package, and pairing/authentication screens in both the web and mobile apps.
- **Specs** (`docs/protocol/local-discovery.md`, `docs/security/local-endpoints.md`): locked design decisions A–G (mDNS advert `_nodus._tcp` on port 9378 with TXT `node_id`/`v`/`pk_fp`, no TLS on the LAN listener, QR deep link `nodus://pair`, web skips active mDNS, hybrid Relay-Push-then-verify token model, HTTP-only transport split for local-auth vs the WS envelope, native-mDNS-first ordering for mobile); threat model covers nonce/`pairing_token` single-use and device-key binding.
- **Protocol** (`packages/protocol`): new `messages/local-discovery.ts`, `messages/local-auth.ts` (`LocalChallengeResponsePayload` includes `nonce`), `messages/pairing.ts` schemas; `PAIRING_TOKEN_PUSH` registered in `envelope.ts` (transport-split decision F — local endpoints are explicitly outside the WS catalog); `SchemaVersionSchema`/`parseVersion` in `version.ts` relaxed to accept `"1.0.0"` (Go/Rust emit patch segments, TS emits `"1.0"`, major/minor equality governs compatibility); `index.ts`/`schema-gen.ts` updated; 34 JSON schemas regenerated. 52 vitest tests pass.
- **Storage Node** (`services/storage-node`): new `src/local/{mod,mdns,server,auth}.rs` + axum listener (`/nodus/discovery`, `/nodus/challenge`, `/nodus/auth`, `/nodus/pair`) wired into `main.rs` behind CORS-permissive middleware; single-use nonce store with expiry; Ed25519 challenge-response verification; pairing fast-path checks local `pairing_sessions` before falling back to the Relay's `/pairing/sessions/verify` (`account_id` placeholder `"local_push"`); migration `20260906000001` adds `paired_at`/`last_authenticated_at` to `devices` and creates `pairing_sessions` (token as BLOB, upsert refreshes expiry); WS `pairing_token_push` arm + `store_pairing_token` in `sync/client.rs`; new Cargo deps `axum 0.8`, `tower-http` (cors), `libmdns 0.10`, `base64 0.22`, `reqwest` json. 67 unit + 1 integration test pass.
- **Relay** (`services/relay`): migration `005_pairing_sessions` (FKs to accounts/nodes/devices, UNIQUE token, status/consumed_at/expires_at, three indexes); `internal/handler/pairing.go` with `CreatePairingSession` (binds token to the device's registered key, 15-min TTL, WS-pushes `pairing_token_push` to the node), `VerifyPairingSession` (single-use, nonce-tolerant), `VerifyNodeURL`; routes `POST /pairing/sessions`, `POST /pairing/sessions/verify`, `GET /nodes/verify`. `go vet` + `go test ./...` clean.
- **relay-client** (`packages/relay-client`): new package with `local-discovery.ts` (`NodeClient`, `fetchAdvertisement`, `nodusBaseUrl`, `parsePairingUrl`, `NodeClientError`), `device-identity.ts` (`createDeviceIdentity`, `identityPrivateKey`/`identityPublicKey`), `ws-client.ts` (`RelayWsClient`, `relayWsEndpoint`); browser-safe base64 (chunked `btoa`, no Buffer). 8 vitest tests pass.
- **Web** (`apps/web`): `/pair` page (relay sign-in → node select → token issue via `POST /devices/register` + `POST /pairing/sessions` → manual-IP `/nodus/discovery` probe → `NodeClient.pair`/`authenticate`), `lib/trusted-nodes.ts` IndexedDB cache, home link; device identity lazy-sourced in `useState` (react-hooks v6 clean). `next build`, `check-types`, `lint` green.
- **Mobile** (`apps/mobile`): pairing screen in `App.tsx` mirroring the web flow; device identity + trusted-node list in the OS keychain via `expo-secure-store` (`src/storage.ts`); `src/relay.ts` thin Relay HTTP client (`EXPO_PUBLIC_RELAY_URL`); `src/compat.ts` pure-JS shims for Hermes-missing globals (`btoa`/`atob`, `TextEncoder`, `AbortSignal.timeout`); `src/discovery.ts` LAN sweep over the device's /24 (Expo Go compatible, decision G); `metro.config.js` enables `unstable_enablePackageExports` + workspace root resolution so `@repo/relay-client`'s `exports` map resolves; `app.json` adds iOS `NSLocalNetworkUsageDescription`/`NSAllowsLocalNetworking` and Android `usesCleartextTraffic` + `expo-secure-store` plugin. `tsc --noEmit` clean; `expo export --platform android` bundles (695 modules).
- **Workspace packaging fix**: catalog TS packages (`@repo/protocol`, `@repo/relay-client`) now compile to `dist` and expose it via `exports` (`types`/`import` conditions) instead of pointing at `src/*.ts`; Turbopack cannot resolve the NodeNext `.js`-suffixed TS subpath imports (Vercel issue #82945), so web `next build` was failing on `./local-discovery.js` — consuming compiled `dist` resolves for both Turbopack (web) and Metro (mobile). `packages/relay-client/tsconfig.json` builds only `src`; `apps/mobile` gained a `build` (`expo export`) and `check-types` script.
**Why:** Phase 11 delivers the previously-missing local trust path: a device on the same network can prove a node is reachable, bind a Relay-issued pairing token to its own key, and authenticate to the node without any certificate provisioning — the baseline for offline-first LAN sync (Phase 12+).
**Impact:** The Storage Node now exposes four unauthenticated LAN endpoints with permissive CORS (browser use) — secured by single-use nonces and token/device-key binding, not network secrecy; the Relay gains pairing-session state and two unauthenticated verification endpoints (token is the credential, mirroring `/buffer/fetch`); both clients can pair against a Relay and against an offline node. Workspace packages now require a build step before apps run (`turbo run build` handles ordering).
**Follow-ups:** Replace the mobile pure-JS /24 sweep with a native mDNS browse (`react-native-zeroconf`/`react-native-dns-sd` via config plugin, needs a dev build — decision G ordering, see `apps/mobile/src/discovery.ts`); add QR-code scanning for the `nodus://pair` deep link on mobile (exp-camera); the web app's mixed-content constraint (`http://` LAN node from `https://` pages) remains out of v1 scope per `docs/security/local-endpoints.md`; `checkAndDeliverPendingShards`/sweep pagination and `relay_http_fetch_url` base-path carve-outs from Phase 10 remain open.

## [2026-09-05] - Phase 10 audit fixes
**What changed:** Closed the security/correctness findings from the Phase 10 implementation audit across the Relay and Storage Node.
- **Tenant isolation (S1)** (`relay/internal/handler/buffer_upload.go`): the `POST /buffer/upload` file-version-existence check now joins `file_versions` to `files` and enforces `f.account_id` — a client can no longer reference another tenant's `(file_id, version_number)` and pollute its own node with foreign rows. Added `TestBufferUploadRejectsForeignVersion`.
- **Stuck-state recovery (C1, C2)** (`relay/internal/buffer/buffer.go`): new `sweepStuckInTransit` reverts `NODE_RECEIVING`/`NODE_VERIFIED` rows older than a 30-min grace back to `RELAY_BUFFERED` (re-adding them to the Redis pending set) so a node that crashes between fetch and ack is re-notified on reconnect instead of stranding the shard forever. Terminal `NODE_STORED` is never touched. Wired into the existing TTL sweep; covered by `sweep_integration_test.go`.
- **Test seed bug (T1)** (`relay/internal/handler/buffer_integration_test.go`): `setupBufferHarness` no longer overwrites its query args with the same two values for every *seed query — the `accounts` row now gets only `account_id` (previously it silently received `(file_id, account_id)`).
- **Closed-channel panic (R2)** (`relay/internal/handler/ws.go`): `checkAndDeliverPendingShards` now sends via `safeSend`, a recover-guarded helper, since it runs outside the hub lock and the client's `Send` channel can be closed mid-backfill.
- **Fetch-URL hardening (D4)** (`storage-node/src/sync/client.rs`, `Cargo.toml`): `relay_http_fetch_url` derives the HTTP endpoint through the `url` crate instead of string surgery, fixing corruption from query strings (`/ws?token=…`), trailing slashes (`/ws/`), and preserving non-`/ws` base paths. Added the `url = "2"` dependency and edge-case tests.
- **Pending-slot orphan guard** (`storage-node/src/sync/client.rs`, `engine.rs`): `record_shard_metadata` re-checks for the `file_versions` row after staging into `pending_shard_fetches` and immediately drains it (via the now-`pub(crate)` `drain_pending_fetches`) if the version synced mid-flight; the re-check is guarded so it never runs the drain against a missing FK target.
- **Docs (Doc1)**: new `docs/architecture/file-state-machine.md` documents the 7-state `file_locations` lifecycle, transition triggers, recovery/redelivery, and invariants; linked from `docs/architecture/README.md`.
**Why:** Audit verdict on Phase 10 was "solid for v1 but fix S1 and C1/C2 before production load": the upload path had a cross-tenant metadata-pollution vector, and a crashed node could permanently strand a buffered shard in a non-terminal state. The remaining items were test integrity, a process-crash panic window, fragile URL derivation, and a latent pending-row race.
**Impact:** Multi-tenant isolation on the buffer upload path is now enforced at the DB query; shard delivery self-heals after node crashes; relay process no longer panics on a client disconnect during pending backfill; node fetch endpoint is robust to odd relay URL configs; state-machine contract is documented for implementers.
**Follow-ups:** Keep `relay_http_fetch_url` base-path semantics in mind if the Relay ever serves the WS and buffer endpoints from different path prefixes; `checkAndDeliverPendingShards` still loads all `RELAY_BUFFERED` rows (no LIMIT) — pagination remains an open tuning item (P2).

## [2026-09-05] - Phase 10: Buffer-and-Relay Transfer (Path C)
**What changed:** Implemented the offline-node shard delivery path end-to-end against the full file state machine (`CREATED → UPLOADING → RELAY_BUFFERED → NODE_RECEIVING → NODE_VERIFIED → NODE_STORED → RELAY_CLEANUP`), no state collapsing. Clients push encrypted shards to the Relay's temporary buffer when the target Storage Node is offline; the node pulls them asynchronously on reconnect, verifies integrity, commits to its object store, and acks the outcome.
- **Protocol** (`packages/protocol`): `shard_upload`/`shard_ack` now carry `version_number` (disambiguates the exact `file_locations` row); `pending_notify` gains `version_number` + single-use `fetch_token`; `size` re-defined as the encrypted wire payload size, not plaintext. Field tables updated in `docs/protocol/message-catalog.md`.
- **Relay schema** (migrations `003`/`004`): `file_locations` gains `hash`/`size_bytes`/`source_device` (needed for `pending_notify` metadata) and a `(node_id, status)` delivery-scan index.
- **Relay HTTP** (`handler/buffer_upload.go`, `handler/buffer_fetch.go`): `POST /buffer/upload` (JWT-authed, X-Nodus metadata headers, BLAKE3 + size verification, upsert-to-`UPLOADING` reservation, FK checks for target-node ownership and version existence, `http.MaxBytesReader` 10 MB cap) and `GET /buffer/fetch` (no JWT — single-use token via Redis `GETDEL`, 10-min TTL; serves bytes, transitions `RELAY_BUFFERED → NODE_RECEIVING`, single-fetch 409 guard).
- **Relay WS/state machine** (`handler/ws.go`, `handler/buffer_notify.go`): `pending_notify` built from row metadata with a fresh fetch token; proactive delivery when the node is online at upload time, reconnect-time delivery via `checkAndDeliverPendingShards` (token re-issued per reconnect since tokens expire while nodes are offline); `shard_ack` `verified` routes `NODE_RECEIVING → NODE_VERIFIED → NODE_STORED` and deletes the buffer copy; `failed` reverts to `RELAY_BUFFERED` for redelivery on next reconnect.
- **Relay sweeper** (`buffer/buffer.go`): TTL sweep now also reaps abandoned `UPLOADING` rows (>1 h grace) to `RELAY_CLEANUP`, keeping the terminal-state invariant; existing `RELAY_BUFFERED` TTL path unchanged.
- **Rust node** (`src/sync/client.rs`, `src/sync/engine.rs`): node sends a `register` envelope after auth (the Relay keys pending delivery on it), handles `pending_notify` → HTTP `GET /buffer/fetch` → BLAKE3-of-body matches `notify.hash`, writes via content-addressed `ObjectStore::put` (object_id must equal the digest), records into `shards` directly or `pending_shard_fetches` when the `file_versions` row hasn't synced yet, then acks `verified`/`failed`. `engine.rs` drains pending fetches into `shards` when `FILE_VERSION_ADDED` lands. Added `reqwest` (HTTP client) and migration `20260905000002_pending_shard_fetches.sql`.
- **Tests:** Relay Postgres integration tests (upload→fetch E2E with single-use token replay rejection, unknown-version/foreign-node/bad-hash rejections, proactive `pending_notify` to an online hub node); Rust unit tests (`relay_http_fetch_url` derivation, pending-fetch drain on version-added, metadata recording direct-vs-pending fallback); updated protocol vitest fixtures for the new required fields.
**Why:** Phase 10 of the implementation plan — the last leg of offline upload: a client must be able to finish syncing while its storage node is powered down, with the Relay holding shards temporarily and the node self-healing on reconnect. Explicit state transitions make multi-node delivery races (double-fetch, stale acks) observable rather than silent.
**Impact:** Full Path C transfer works across the three stacks; offline nodes recover pending shards automatically; the `file_locations` status column is now the single source of truth for the whole 7-state machine; single-use fetch tokens bound replay exposure to a 10-minute window.
**Follow-ups:** Wire-level e2e driving the real Rust `run_sync_session` against the Relay (handlers are currently tested directly); decrypt-and-verify on the client once decryption lands; backoff on node reconnect; sweep notification for `RELAY_CLEANUP` rows so clients can surface abandoned shards.

## [2026-09-05] - Phase 9 verification fixes
**What changed:** Hardened the Phase 9 snapshot rebuild path after a code review of the Go Relay handlers. Closed five issues:
- **Data race:** `HandleSnapshotBegin` now consults a mutex-guarded helper (`hasActiveSessionForAccount`) instead of iterating the shared `rebuildSessions` map bare, so the single-flight check no longer races concurrent session mutations (`handler/snapshot.go`).
- **Abort → failed status:** `abortRebuildSession` now flips an in-flight (`delivered`) `rebuild_requests` row to `failed` (single-flight guarantees at most one per node), so operators can see a rebuild outcome instead of a permanent `delivered` (`handler/snapshot.go`).
- **Reason validation:** `POST /rebuild` rejects reasons outside the protocol enum (`admin | restore | schema_mismatch`) with `400` instead of storing/forwarding junk; validation extracted into a pure `validateRebuildReason` and unit-tested (`handler/rebuild.go`, `rebuild_test.go`).
- **Retention:** added `handler/rebuild_prune.go` (`pruneExpiredRebuildRequests`) — hourly sweep deletes `delivered`/`failed` `rebuild_requests` older than 30 days, wired in `main.go` next to the tombstone pruner; pending rows are never pruned.
- **Test integrity:** the out-of-order-chunk integration test now sends a *signed* BEGIN so the session actually opens and the ordering abort path is genuinely exercised (previously it passed because the unsignd BEGIN was rejected before the session existed). Added integration coverage for failed-status marking and prune behavior.
**Why:** Verification found a `-race`-detectable map race, a test that passed for the wrong reason, and three operational gaps (unbounded `rebuild_requests`, invisible failures, unvalidated input) that would have caused confusing behavior in production.
**Impact:** All `services/relay` handlers are race-clean (`go test -race` green), the request table is bounded at 30 days, aborts are observable, and the integration suite now covers the failure paths it claimed to.
**Follow-ups:** The pending/delivered/failed lifecycle is heuristic (most recent `delivered` row per node); replace with an explicit request↔snapshot correlation ID if multi-snapshot-per-node retries ever need per-request attribution.

## [2026-09-05] - Phase 9: Full Snapshot / Relay Rebuild
**What changed:** Implemented the Phase 9 full-snapshot rebuild path across all three stacks.
- **Protocol** (`packages/protocol`): rewrote `src/messages/snapshot.ts` with typed, homogeneous chunks (`SNAPSHOT_CHUNK_MAX_RECORDS=1000`, `record_type` `file_version|tombstone`), `SnapshotBeginPayloadSchema` carrying `snapshot_sequence` + full `{origin_id → origin_sequence}` cursor map, and `SnapshotEndPayloadSchema`. Added `src/messages/rebuild.ts` (`REBUILD_REQUIRED` with `node_id`/`reason`); wired it into the envelope dispatch; regenerated all 24 JSON schemas; updated tests.
- **Relay schema / routing**: migration `002_snapshot_rebuild.sql` adds `storage_nodes.is_primary` (auto-set `true` on an account's first node), a durable `rebuild_requests` queue, and account-scoped staging `rebuild_files`/`rebuild_file_versions`/`rebuild_tombstones`. `RegisterNode` sets primary-on-first-insert. New `POST /rebuild` trigger endpoint routes `REBUILD_REQUIRED` to the account's online primary, else queues it (delivered on reconnect). Added an hourly tombstone prune (90 days per ADR-0005).
- **Rust node**: new `src/sync/snapshot.rs` builds a snapshot with a monotonic `snapshot_sequence`, full cursor map, BLAKE3 content hash, and typed homogeneous chunks; `client.rs` streams BEGIN/CHUNK/END and handles `rebuild_required`.
- **Go Relay ingestion**: `handler/snapshot.go` implements BEGIN (schema major-compat, Ed25519 signature, primary-role, single-flight-per-account) / CHUNK (strict ordered indexing, staging, raw-record accumulation) / END (signature + recomputed BLAKE3 `content_hash` match, then atomic promotion). `handler/promote.go` drops the §22 cascade FKs, does a per-account DELETE+INSERT replace, restores explicitly-named FKs, resets `sync_cursors` to the snapshot checkpoint (leaving durable `sync_events` intact), and prunes orphaned buffer/envelope rows without touching on-disk buffer files. Aborts clear staged data entirely. Added `github.com/zeebo/blake3`; cross-language hash vector; DB-free unit tests + Postgres integration tests (promotion/§22, happy-path, hash-mismatch abort, out-of-order abort).
**Why:** Phase 9 of the implementation plan — let the Relay fully rebuild its projection from a Storage Node's snapshot after a wipe or schema mismatch, with verifiable integrity and without violating the §22 "buffer entry not in snapshot ≠ delete" rule.
**Impact:** Relay can now recover from full data loss; `REBUILD_REQUIRED`/`snapshot_*` messages added to the wire contract; `is_primary` and `rebuild_*` schema; Rust build/stream; Go ingestion + atomic promotion.
**Follow-ups:** Break the snapshot flow into the Rust `stream_snapshot` ↔ Go `HandleSnapshot*` wiring behind live websockets (tests currently call handlers directly); make public-key/signature handling shared via the scheme constants; add a wire-level e2e that drives the Rust node's real `stream_snapshot` against the Relay.

## [2026-09-04] - Phase 7: Go Relay Control Plane (`services/relay`)
**What changed:** Implemented the Go Relay control plane under `services/relay`. Set up PostgreSQL (via `pgxpool` and `golang-migrate` embedded migrations) and Redis (via `go-redis` with presence tracking). Built HTTP REST endpoints for unauthenticated access (`/auth/register`, `/auth/login`, `/auth/refresh`) using Argon2id and JWTs + opaque refresh tokens, and authenticated access (`/auth/logout`, `/devices`, `/nodes`) using `context`-based middleware. Implemented the WebSocket Hub (`hub.go`) handling connection lifecycle and message dispatch based on the Nodus wire protocol, including heartbeat presence refresh. Implemented the Relay Buffer (`buffer.go`) for transient shard storage with atomic disk writes and a background TTL sweeper. Fixed SQL queries (version number scoping) and Hub memory leaks. Added unit tests for auth, config, buffer, and hub logic.
**Why:** Phase 7 of the implementation plan — the Relay handles device/node authentication, coordinates WebRTC signaling (to be added), acts as the central control plane, and provides transient buffer storage for offline storage nodes.
**Impact:** `services/relay` now contains a fully functional HTTP API and WebSocket gateway, communicating with PostgreSQL 17 and Redis 7 (configured via `docker-compose.yml`).
**Follow-ups:** Implement WebRTC signaling messages and the Relay-Client TypeScript library (Phase 8).## [2026-09-04] - Phase 6: Storage Node Object Store (`services/storage-node`)
**What changed:** Implemented content-addressed object store under `src/store/` with `layout.rs` (bucket prefix scheme `<data_dir>/objects/<prefix>/<hash>`), `write.rs` (atomic temp-write-then-rename, fsync durability, SQLite `storage_objects` tracking, and startup crash recovery), `reconcile.rs` (Phase A metadata-to-disk scan with DEGRADED status marking on missing/corrupted objects, Phase B disk-to-metadata orphan scan with 24-hour grace period deletion per ADR-0005, and 24h periodic background task), `gc.rs` (ADR-0005 garbage collection job retaining 5 most recent versions or <=30 day versions, cascading unreferenced shard object cleanup, 90-day tombstone compaction, and 6h periodic background task), and `mod.rs` (re-exports). Added `blake3`, `uuid`, `walkdir`, and `chrono` dependencies. Updated `main.rs` to initialize the store, run crash recovery, spawn non-blocking background tasks, and listen for termination signals (`tokio::signal::ctrl_c`). Added 18 unit tests (48 total in crate).
**Why:** Phase 6 of the implementation plan — provide durable content-addressed storage for encrypted shards on disk, ensure crash consistency and physical-to-metadata reconciliation (§21 / §21a), and implement the automated garbage collection policy (§29a / ADR-0005).
**Impact:** `services/storage-node` now has fully functional on-disk shard storage, reconciliation, and automated GC.
**Follow-ups:** Wire network transfer endpoints (Phase 10 / Phase 12) to write and read from this store.

## [2026-09-03] - Storage Node: Configurable Backup Directory
**What changed:** Introduced a run-time configurable data directory for the Rust Storage Node. Added `src/config/` with `mod.rs` (first-run detection, `--data-dir`/`NODUS_DATA_DIR` precedence, `~/.nodus/config.toml` read/write), `prompt.rs` (dialoguer interactive first-run flow with `~/NodusBackup` default and create-if-missing), and `validation.rs` (writable-path probe, cloud-sync folder detection, removable/network mount detection, prior-install marker check). Wired config resolution into `src/main.rs` via `clap` (`--data-dir`). Added `dialoguer`, `directories`, `serde`, `toml`, `clap` dependencies. Documented the fixed/user-chosen split in `nodus_implementation_plan.md` §11 and the first-run flow in new §11a; updated the object-store path to `<data_dir>/objects/<prefix>/`; added Phase 5a to `Todo.md`. All relative to the existing docs-only design (the `identity` subdir is reserved for the later node-keypair task).
**Why:** Phase 5a — the node should prompt for a local data directory on first run instead of hardcoding `~/.nodus/objects/`, with unattended-install support so daemon restarts never block, and safeguards against adopting a prior install or a cloud/removable location.
**Impact:** New `src/config/` module and `src/main.rs` boot wiring in `services/storage-node`. New CLI `--data-dir` flag and `NODUS_DATA_DIR` env var. `node.db` is now `<data_dir>/nodus.db`; objects live under `<data_dir>/objects/<prefix>/`. Adds four dependencies to the storage-node crate.
**Follow-ups:** v1 scope boundary — changing `data_dir` after setup is out of scope and needs a migrate-and-verify flow, not a config edit (TODO noted in `docs/architecture`-adjacent plan §11a). Identity subdir task (Phase 5) will populate `~/.nodus/identity/`.

## [2026-09-02] - Phase 4: Protocol Package (`packages/protocol`)
**What changed:** Created the `@repo/protocol` package (packages/protocol) defining the canonical Nodus wire protocol: `src/envelope.ts` (base envelope + `parseMessage` dispatch), `src/version.ts` (schema version constants + compatibility helpers), `src/errors.ts` (generic error envelope, error codes, `ProtocolError`), and per-category message schemas in `src/messages/{control,webrtc,transfer,sync,snapshot}.ts`. Finalized the sync event schema and ordering fields (`src/events/event-types.ts`) with an `EventType` enum and per-type payload validation. Added protocol-local branded types with mapping helpers (`src/types.ts`) so the package is zero-dependency on `@repo/core`. Added a JSON Schema generator (`src/schema-gen.ts`, `pnpm generate:schemas`) emitting `schemas/*.schema.json` for Rust/Go reference. Added 35 vitest round-trip/rejection tests in `tests/`. Wrote `docs/protocol/schema-versioning.md`, `message-catalog.md`, and `event-types.md`, and updated `docs/protocol/README.md` and `Todo.md`.
**Why:** Phase 4 of the implementation plan — define all protocol message types with runtime (zod) validation rather than TS types alone, document the schema versioning strategy, and resolve the "exact sync event schema" and "event ordering guarantees" open items before Phase 8 consumes them.
**Impact:** New package `packages/protocol` (zod v4 runtime-validation, no dependency on core). Adds `schemas/*.json` generated artifacts and `docs/protocol/` documentation. Unblocks Phases 5+ (Rust node, Go relay, relay-client) which build against this contract.
**Follow-ups:** Rust (`serde`) and Go (`encoding/json`) hand-written equivalents must mirror these schemas per `docs/protocol/schema-versioning.md`; regenerate `schemas/` whenever a schema changes.
