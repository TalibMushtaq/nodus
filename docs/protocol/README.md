# Protocol

Canonical protocol specifications, message schemas, and versioning strategy.

Will be populated in Phase 4 (`packages/protocol` implementation) based on `nodus_implementation_plan.md` §4.
